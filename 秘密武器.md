# 平台侧关键信息摘录

本文用于记录从平台 `tools/`、环境封装、评分器、监控与观测链路中挖出的比赛可用信息。  
格式固定为：

```text
{
关键内容:
解读:
具体代码:
引用:
}
```

## 第一批：base_env / base_scorer / critic observation / monitor

### 1. 评估相机只影响录像，不影响得分

```text
{
关键内容:
平台评估录像使用跟随相机，默认 eye_offset=(-1.7, 0.0, 1.5)，target_offset=(0.3, 0.0, 0.25)，并使用 EMA 平滑 alpha=0.2。

解读:
这部分只影响评估 MP4 的观察视角，不参与机器人控制、奖励或最终评分。它的价值主要是：如果视频里看不到完整狗，或者首帧/终止帧异常，可以知道视频生成逻辑是平台侧跟随 root pose 的结果。

具体代码:
DEFAULT_EVAL_CAMERA_EYE_OFFSET = (-1.7, 0.0, 1.5)
DEFAULT_EVAL_CAMERA_TARGET_OFFSET = (0.3, 0.0, 0.25)
DEFAULT_EVAL_CAMERA_SMOOTHING_ALPHA = 0.2

引用:
来源文件：tools/base_env/base_env.py
函数：get_default_eval_camera_profile / compute_follow_camera_view / smooth_follow_camera_view
}
```

### 2. 算法 feature 模块由 KAIWU_ALGORITHM 动态决定

```text
{
关键内容:
平台不会硬编码只用 agent_ppo，而是读取环境变量 KAIWU_ALGORITHM，动态导入 agent_{algo}.feature 中的 CriticObservationProcess、PolicyObservationProcess、RewardProcess。

解读:
如果提交 diy，必须保证 agent_diy.feature 里导出的三个类完整可用；如果提交 ppo，则用 agent_ppo.feature。观测和奖励是否生效，取决于这个动态导入链路，而不是文件是否存在。

具体代码:
algo = os.environ.get("KAIWU_ALGORITHM", "ppo")
module_name = f"agent_{algo}.feature"
module = importlib.import_module(module_name)
return module.CriticObservationProcess, module.PolicyObservationProcess, module.RewardProcess

引用:
来源文件：tools/base_env/base_env.py
函数：_get_algo_feature
}
```

### 3. 标准模式 curriculum=false 并不等于完全随机地形生成

```text
{
关键内容:
standard 模式下，如果 curriculum=false 且不是 eval，平台仍会把 terrain_generator.curriculum 设为 True，但关闭运行期升降级，并在 reset 时均匀采样 terrain_levels。

解读:
这是为了保留“行=难度、列=地形类型”的确定性网格，同时避免训练期自动升降级。对训练有用：想固定难度分布但不让 curriculum 自动调难度时，可以用 curriculum=false；但这并不是 Isaac Lab 原生的完全 random terrain 行为。

具体代码:
standard_static_grid = normalized_mode == "standard" and not curriculum_enabled and not is_eval_mode
if standard_static_grid:
    tg.curriculum = True
    scene_terrain.randomize_terrain_levels_on_reset = True
    scene_terrain.terrain_level_sampling_mode = "uniform"

引用:
来源文件：tools/base_env/base_env.py
函数：_configure_terrain_curriculum_behavior
}
```

### 4. 评估模式的地形 seed 与环境随机 seed 是两套逻辑

```text
{
关键内容:
评估模式会把 env_conf.seed 或 terrain.seed 透传给 terrain_generator.seed，使地形布局稳定；同时 env_cfg.seed 来自 RANDOM_SEED，每轮可能不同。

解读:
正式评估可能做到“地形瓦片布局稳定，但物理/随机过程每轮不同”。训练时 terrain_generator.seed 不会从 env_conf.seed 透传，训练地形布局不被 seed 固定。想复现实验，需要同时关注 terrain seed 和 env seed。

具体代码:
if is_eval_for_seed:
    terrain_seed = terrain_conf.get("seed", usr_conf.get("env_conf", {}).get("seed", getattr(env_cfg, "seed", None)))
    tg.seed = int(terrain_seed)
    env_cfg.seed = _resolve_random_seed() & 0x7FFFFFFF
else:
    _log("[terrain] Train mode: skip terrain_generator.seed transfer")

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 5. track 模式会替换 reset_base，从赛道起点生成

```text
{
关键内容:
terrain.mode 为 track 时，平台会把 reset_base.func 切换为 reset_root_state_track_start。

解读:
Track 训练和评估的出生点逻辑与 standard 不同。Track 模型如果在训练时没有适应“赛道起点生成”，考试时可能一开始就方向/位置策略不匹配。

具体代码:
if mode == "track" and hasattr(env_cfg, "events") and hasattr(env_cfg.events, "reset_base"):
    from unitree_rl_lab.tasks.locomotion.mdp.events import reset_root_state_track_start
    env_cfg.events.reset_base.func = reset_root_state_track_start

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 6. 用户 TOML 可以透传 standard 子地形参数

```text
{
关键内容:
standard_conf 中对子地形配置的字段会被透传到对应 sub_terrain cfg；如果指定了任何 proportion，平台会先把所有子地形 proportion 清零，再应用用户配置。

解读:
训练配置里的地形比例是强覆盖，不是叠加。想只训练某几类地形时，写 proportion 很有效；没写到的地形会被清零，避免默认地形比例残留。

具体代码:
if has_proportion:
    for sub_name, sub_cfg in tg.sub_terrains.items():
        sub_cfg.proportion = 0.0

for sub_name, sub_params in standard_conf.items():
    sub_cfg = tg.sub_terrains[sub_name]
    setattr(sub_cfg, param_key, param_val)

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 7. track 配置里的 track_length 会覆盖 num_rows

```text
{
关键内容:
track.track_length 会写入 tg.track_length，同时强制 tg.num_rows = track_length。num_parallel_tracks 会强制覆盖 num_cols。

解读:
Track 模式中 num_rows/num_cols 的语义与 standard 不同：track_length 是赛道段数，num_parallel_tracks 是并行赛道数量。训练配置里只改 terrain.num_rows 不一定有效，关键要看 [terrain.track]。

具体代码:
tl = track_conf.get("track_length")
if tl is not None:
    tg.track_length = int(tl)
    tg.num_rows = int(tl)

npt = track_conf.get("num_parallel_tracks")
if npt is not None:
    tg.num_parallel_tracks = int(npt)
    tg.num_cols = int(npt)

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 8. 评估支持 level 列表，且会强制确定性放置

```text
{
关键内容:
评估模式下，如果 [terrain] level = [...] 存在，平台会排序、去重、clamp 到 [0, 9]，并把 level 列表挂到 env_cfg 上。standard 会按 level 和 sub_terrains 重建确定性网格；track 会确保 num_parallel_tracks 覆盖最大 level。

解读:
这是复盘正式评估最重要的信息之一。考试很可能通过 level 列表指定难度集合，而不是靠训练 curriculum 随机抽。训练时应专门覆盖这些 level，尤其是高 level。

具体代码:
level_list_int = sorted(set(max(0, min(9, v)) for v in raw_level_list))
env_cfg._eval_level_list = list(level_list_int)

if mode == "standard":
    tg.num_rows = max_level_required
    tg.num_cols = len(sub_list)
    tg.sub_terrains = new_sub_terrains

elif mode == "track":
    if tg.num_parallel_tracks < max_level_required:
        tg.num_parallel_tracks = max_level_required
        tg.num_cols = max_level_required

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 9. eval level 模式会保留 tg.curriculum=True，但禁用运行期 terrain_levels curriculum

```text
{
关键内容:
评估 level list 模式下，平台强制 tg.curriculum=True，以便 Isaac Lab 使用 curriculum terrain generation 的固定列映射；同时把 env_cfg.curriculum.terrain_levels 置 None，避免运行期升降级。

解读:
这里的 curriculum=True 不是训练意义上的课程学习，而是为了让地形生成具有固定 col→sub_terrain 映射。不要误以为考试还在自动升降难度；难度放置由 reset_root_state_eval_level_aware 决定。

具体代码:
if hasattr(tg, "curriculum") and not tg.curriculum:
    tg.curriculum = True

if hasattr(env_cfg.curriculum, "terrain_levels"):
    env_cfg.curriculum.terrain_levels = None

env_cfg.events.reset_base.func = reset_root_state_eval_level_aware

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 10. 命令速度范围分为 ranges 和 limit

```text
{
关键内容:
[commands.ranges] 会写入 base_velocity.ranges；[commands.limit] 会写入 base_velocity.limit_ranges。

解读:
ranges 是当前采样范围，limit 是 curriculum 能扩展到的硬上限。训练时如果只扩大 limit 但 ranges 很小，早期策略仍然只看到低速命令；如果 ranges 太大，初期可能不稳定。

具体代码:
base_vel_cmd.resampling_time_range = (...)
setattr(cmd_ranges, attr_name, (float(val[0]), float(val[1])))
setattr(limit_ranges, attr_name, (float(val[0]), float(val[1])))

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 11. domain randomization 真实控制 friction、base mass、push

```text
{
关键内容:
domain_rand.enable_domain_rand 会统一影响摩擦随机化、base mass 随机化和 push_robot。关闭时摩擦被固定为 [1.0, 1.0]，push velocity 置 0。

解读:
如果考试开启 domain random，训练最好保留相近扰动；如果为了收敛短期关闭 domain random，模型可能在正式评估摩擦和推力变化下失稳。

具体代码:
if not enable_domain_rand or not randomize_friction:
    static_friction_range = (1.0, 1.0)
    dynamic_friction_range = (1.0, 1.0)

if not enable_domain_rand or not push_robots:
    velocity_range = {"x": (0.0, 0.0), "y": (0.0, 0.0)}

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 12. policy observation noise 可以由 TOML 控制

```text
{
关键内容:
[noise].add_noise=false 会关闭 policy observation corruption；开启时可以分别配置 dof_pos、dof_vel、ang_vel、gravity 噪声，并乘以 noise_level。

解读:
噪声只作用在 policy observation，不一定作用在 critic。训练时开噪声能提升鲁棒性，但会降低初期收敛速度；如果考试观测有噪声，关闭训练噪声可能过拟合。

具体代码:
if not add_noise:
    policy_obs.enable_corruption = False
else:
    policy_obs.enable_corruption = True
    obs_term.noise = Unoise(n_min=-scaled_noise, n_max=scaled_noise)

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 13. isaaclab_override / env_cfg_override 是最终原始覆盖口

```text
{
关键内容:
usr_conf 中的 [isaaclab_override] 或 [env_cfg_override] 会在最后递归覆盖 env_cfg 对象字段。

解读:
这是一个高权限配置入口。理论上可以覆盖默认 env_cfg 中很多未暴露字段，但风险也大：写错字段会被跳过，写对但语义不清可能导致训练和评估不一致。

具体代码:
for override_key in ("isaaclab_override", "env_cfg_override"):
    override_conf = usr_conf.get(override_key, {})
    if override_conf:
        _apply_nested_overrides(env_cfg, override_conf, path=override_key)

引用:
来源文件：tools/base_env/base_env.py
函数：apply_usr_conf_to_env_cfg
}
```

### 14. 奖励项由 TOML 全量注册到 Isaac Lab reward_manager

```text
{
关键内容:
平台解析 [rewards.*]，得到 term_name、weight 和 params，然后通过 RewardProcess.create_bridge_from_configs 注册到 env_cfg。

解读:
训练 reward 是否生效取决于 TOML 里是否写了对应项，以及 RewardProcess 是否有同名 _reward_xxx 方法。最终评分不是 reward，但 reward 分项会进入监控，有利于判断训练方向。

具体代码:
result[term_name] = {
    "weight": float(weight),
    "params": params,
}

reward_process = RewardProcess()
bridge = reward_process.create_bridge_from_configs(reward_configs)
bridge.register_to_env_cfg(env_cfg)

引用:
来源文件：tools/base_env/base_env.py
函数：parse_reward_configs / _create_env
}
```

### 15. 自定义 policy / critic observation 会覆盖 Isaac Lab observation group

```text
{
关键内容:
平台会创建 PolicyObservationProcess 和 CriticObservationProcess，并通过 ObservationBridge 覆盖 env_cfg 中对应 observation group。

解读:
观测维度不是只看 Isaac Lab 默认配置，还要看 agent_xxx.feature 里的 process()。如果改模型输入维度，必须和这两个 process 的输出严格对齐。

具体代码:
for observation_process in (PolicyObservationProcess(), CriticObservationProcess()):
    observation_process.create_bridge().override_group_in_env_cfg(env_cfg)

引用:
来源文件：tools/base_env/base_env.py
函数：register_observation_processes
}
```

### 16. 评估录像只在 eval 且 save_mp4=true 时启用

```text
{
关键内容:
enable_video = is_eval and save_mp4。并且 num_envs <= 16 时启用 TiledCamera，为每个 env 单独录制跟随视频。

解读:
如果想通过视频诊断策略，评估配置需要 save_mp4=true 且环境数量不要超过 16。训练阶段默认不会录视频。

具体代码:
self.is_need_save_mp4 = bool(env_conf.get("save_mp4", False))
self.enable_video = self.is_eval and self.is_need_save_mp4
self._use_tiled_camera = self.enable_video and env_cfg.scene.num_envs <= 16

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.reset / Robot._create_env
}
```

### 17. 评估模式会启用单次生命统计

```text
{
关键内容:
评估模式中，每个 env 首次 done 后会被标记为完成，后续 action 被置零，scorer 也只统计首次完成。

解读:
正式评估不是让同一个 env 无限刷多次 episode 求平均，而是每个 env 一次生命。首个 episode 的成功/失败非常关键；auto-reset 后的数据不会继续帮你补分。

具体代码:
if self.is_eval and self._eval_env_done_mask is not None:
    actions[self._eval_env_done_mask] = 0.0

newly_done = dones_bool & ~self._eval_env_done_mask
self._eval_env_done_mask[newly_done_ids] = True
dones_for_scorer = newly_done

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.step
}
```

### 18. scorer 需要 step 前快照，避免 auto-reset 污染 episode 步数和难度

```text
{
关键内容:
Robot.step 在 env.step(actions) 前保存 episode_length_buf 和 terrain_levels，传给 EnvMonitor/BaseScorer。

解读:
Isaac Lab step 内部会 auto-reset 并更新 terrain_levels。平台特意拍快照说明最终分组和时间分数使用的是“done 前真实 episode”的步数和难度，不是 reset 后的新状态。

具体代码:
pre_step_episode_lengths = env_unwrapped.episode_length_buf.clone()
pre_step_terrain_levels = terrain.terrain_levels.clone()

self._env_monitor.on_step(
    env_unwrapped,
    dones_for_scorer,
    pre_step_episode_lengths=pre_step_episode_lengths,
    pre_step_terrain_levels=pre_step_terrain_levels,
)

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.step
}
```

### 19. Track 模式主动维护 goal_positions

```text
{
关键内容:
Robot.step 在 track 模式下主动调用 ensure_goal_positions_ready 和 update_goal_positions，不依赖 observation_process 是否包含 goal term。

解读:
即使用 standard 训练出的无 goal 观测模型去跑 track，平台也会维护 goal_positions 供 scorer、termination、reward 使用。但策略本身如果看不到 goal，导航能力仍然可能不足。

具体代码:
if self._task_type == "track":
    from tools.base_env.observation_process import ensure_goal_positions_ready, update_goal_positions
    if ensure_goal_positions_ready(env_unwrapped):
        update_goal_positions(env_unwrapped)

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.step
}
```

### 20. 评估终止条件：所有 env 首次完成、所有 env terminated、或超过 max length

```text
{
关键内容:
Robot.step 中触发 all_done 的条件包括：评估模式所有 env 都完成过首次 episode；所有 env terminated；frame_no > max_episode_length。

解读:
评估整体结束不一定等每个环境跑满时间。只要所有 env 都首次 done，就会生成结果文件。策略早摔会很快结束，并且低分会被写入最终 JSON。

具体代码:
eval_all_envs_done = self.is_eval and self._eval_env_done_mask is not None and bool(self._eval_env_done_mask.all())
reached_max_length = self.frame_no > self.max_episode_length

if eval_all_envs_done or all_terminated or reached_max_length:
    self.make_json_and_done_file()
    infos["all_done"] = True

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.step
}
```

### 21. episode_length_s 会换算成 max_episode_length，步长固定按 0.02s 算

```text
{
关键内容:
reset 中如果配置 episode_length_s，则 max_episode_length = episode_length_s / 0.02。注释说明 dt=0.005、decimation=4。

解读:
时间分数的 max_steps 来自这里。比如 episode_length_s=25，则 max_episode_length=1250；episode_length_s=150，则 max_episode_length=7500。速度策略是否“够快”，要按这个上限换算。

具体代码:
episode_length_s = env_section.get("episode_length_s")
if episode_length_s is not None:
    step_dt = 0.02
    self.max_episode_length = int(float(episode_length_s) / step_dt)

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.reset
}
```

### 22. 最终结果写入 result JSON 的 end_info

```text
{
关键内容:
评估结束时，平台从 BaseScorer.get_lifetime_metrics() 构建 end_info，并写入 /workspace/battle/{game_id}/result/{game_id}.json。

解读:
最终平台展示/判分的数据来自 lifetime metrics，不受 60 秒监控 flush 清空影响。想定位最终分数，应重点看 result JSON 的 end_info，而不是训练 reward。

具体代码:
metrics = self._scorer.get_lifetime_metrics()
end_info_str = json.dumps(end_info_dict, ensure_ascii=False)
json_file = f"{self.battle_dir}/result/{self.game_id}.json"

引用:
来源文件：tools/base_env/base_env.py
函数：Robot._build_end_info / Robot.make_json_and_done_file
}
```

### 23. standard 的 episode_count 不重复累加 timeout

```text
{
关键内容:
_build_end_info 中 track 的 episode_count = completed + abnormal + timeout；standard 的 episode_count = completed + abnormal，因为 timeout 是 abnormal 的子类。

解读:
看 standard 监控时不能把 completed、abnormal、timeout 三者直接相加，否则会重复计算 timeout。判断成功率应使用 completed / (completed + abnormal)。

具体代码:
if self._task_type == "track":
    return c + a + t
return c + a

引用:
来源文件：tools/base_env/base_env.py
函数：Robot._build_end_info
}
```

### 24. track 的最终详情按难度列分组，不按子地形展开

```text
{
关键内容:
track 模式下 score_detail 使用 track_level_{col}，export_score_detail 与 score_detail 相同；standard 才按 terrain_type 和 terrain_type_level 展开。

解读:
Track 是整条赛道作为一个整体评分，不能单独看到每段 slope/stairs/maze 的最终得分；只能看到不同难度列的结果。训练分析时要额外靠视频或自定义 reward/monitor 判断是哪一段失败。

具体代码:
if self._task_type == "track":
    for col in range(self._scorer._num_cols):
        "terrain_type": f"track_level_{col}"
    export_score_detail = list(score_detail)

引用:
来源文件：tools/base_env/base_env.py
函数：Robot._build_end_info
}
```

### 25. BaseScorer 是最终平台评分核心

```text
{
关键内容:
BaseScorer 统计 completed / abnormal / timeout、total_score、step_score、forward_score、pose_score、energy_score，并按地形和难度分组。

解读:
这是最终评分口径，不是训练 reward。训练 reward 只是优化手段；最终分数取决于 BaseScorer 的 episode 级聚合。

具体代码:
episode_total_scores
episode_step_scores
episode_forward_scores
episode_pose_scores
episode_energy_scores

引用:
来源文件：tools/base_env/base_scorer.py
类：BaseScorer
}
```

### 26. standard 总分不乘完成系数，track 总分乘 completion_coeff

```text
{
关键内容:
standard 总分 = forward_score*0.4 + time_score*0.2 + energy_score*0.2 + pose_score*0.2。track 总分 = completion_coeff * (time_score*0.4 + pose_score*0.4 + energy_score*0.2)。

解读:
Standard 即使没完全走穿，也可能靠 forward/pose/energy 拿部分分；但 time_score 未走穿为 0。Track 只要没 goal_reached，总分直接归 0，姿态和能耗再好也没用。

具体代码:
total_score = (
    forward_score * self.STD_FORWARD_WEIGHT
    + time_score * self.STD_TIME_WEIGHT
    + energy_score * self.STD_ENERGY_WEIGHT
    + pose_score * self.STD_POSTURE_WEIGHT
)

total_score = completion_coeff * (
    time_score * self.TRACK_TIME_WEIGHT
    + pose_score * self.TRACK_POSTURE_WEIGHT
    + energy_score * self.TRACK_ENERGY_WEIGHT
)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.on_step
}
```

### 27. 姿态分与能耗分的确切公式

```text
{
关键内容:
pose_score = 100 * exp(-5 * mean_deviation)，mean_deviation = episode 平均(|roll| + |pitch|)。energy_score = 100 * exp(-0.01 * mean_energy)，mean_energy = episode 平均(sum(|joint_vel| * |applied_torque|))。

解读:
跳跃/冲刺只有在显著减少时间且不大幅增加 roll/pitch 和机械功率时才划算。pose_score 对 roll/pitch 很敏感，energy_score 对高扭矩高速度动作敏感。

具体代码:
pose_deviation = torch.abs(roll) + torch.abs(pitch)
energy_per_env = torch.sum(torch.abs(joint_vel) * torch.abs(applied_torque), dim=-1)

return 100.0 * math.exp(-5.0 * mean_deviation)
return 100.0 * math.exp(-0.01 * mean_energy)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step / _compute_posture_score / _compute_energy_score
}
```

### 28. standard 前进分按出生点 2D 最远距离，不看方向

```text
{
关键内容:
standard 的 d_forward 是 episode 内 max(||current_xy - episode_start_xy||_2)，不区分方向；归一化长度是地形块 X 长度的一半。

解读:
Standard 的前进距离不是纯世界 X 正向位移，而是离出生点的 2D 欧氏最远距离。理论上横向移动也会涨 forward_score，但越界和地形目标会限制这种投机空间。

具体代码:
dist_from_start = torch.norm(delta_xy, dim=-1)
self._forward_accum = torch.maximum(self._forward_accum, gated_dist)

if self._task_type == "standard":
    return self._terrain_length_x / 2.0

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step / _forward_score_length
}
```

### 29. standard 走穿阈值是半块长度减 0.1

```text
{
关键内容:
standard 走穿条件为 forward_accum >= terrain_length_x / 2 - 0.1。首次走穿的物理步数用于 time_score。

解读:
Standard 不需要等 episode 结束才算时间分；越早达到走穿阈值，time_score 越高。训练上应鼓励尽快离出生点达到半块边界附近，而不是跑满全时长。

具体代码:
return self._terrain_length_x / 2.0 - 0.1

if just_traversed.any():
    self._traverse_step[just_traversed] = self._physics_step_count[just_traversed]
    self._traversed[just_traversed] = True

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._traversal_threshold / _accumulate_per_step
}
```

### 30. Track 必须依赖 goal_reached，缺失会直接报错

```text
{
关键内容:
BaseScorer 在 track 模式构造时强校验 termination_manager.active_terms 必须包含 goal_reached；on_step 首次 done 时还会检查 env.goal_positions 不是 None/全零。

解读:
Track 的完成判定非常硬：goal_reached 是唯一完成依据。若 goal_positions 维护失败，平台宁愿报错也不静默给错分。训练和评估都要确认 goal_reached term 正常触发。

具体代码:
if "goal_reached" not in active_terms:
    raise RuntimeError(...)

if goal_positions is None or max_abs < 1e-6:
    raise RuntimeError(...)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.__init__ / BaseScorer.on_step
}
```

### 31. Track 完成才 completed，失败或超时都不会拿总分

```text
{
关键内容:
track 中 completion_coeff = 1.0 if is_goal_reached else 0.0；计数上 timeout、completed、abnormal 三类互斥。

解读:
Track 排名核心先看能不能到终点。没到终点，姿态分和能耗分不会贡献 total_score。策略设计要优先保证到达，再优化姿态/能耗/时间。

具体代码:
completion_coeff = 1.0 if is_goal_reached else 0.0

if is_timeout:
    timeout_count += 1
elif is_goal_reached:
    completed_count += 1
else:
    abnormal_count += 1

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.on_step
}
```

### 32. standard 的 timeout 是 abnormal 子类

```text
{
关键内容:
standard 中 completed 表示走穿，abnormal 表示未走穿的一切情况，timeout 只在 abnormal 且 is_timeout 时额外 +1。

解读:
看 standard 指标时，timeout_count 不是第四类，也不是与 abnormal 互斥的一类。失败原因分析可以看 timeout，但成功率分母不要重复加 timeout。

具体代码:
if bool(self._traversed[env_idx].item()):
    completed_count += 1
else:
    abnormal_count += 1
    if is_timeout:
        timeout_count += 1

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.on_step
}
```

### 33. Track 按 terrain_types 列索引统计难度档

```text
{
关键内容:
Track 模式按 terrain.terrain_types 的列索引 col 作为难度档，输出 total_score_track_l{col} 等指标。

解读:
Track 的难度分组不是 terrain_levels，而是列索引。难度中心约为 difficulty_range 内按 col 均匀划分。分析训练曲线时应看 track_l0、track_l1 ... 而不是子地形名。

具体代码:
done_cols = env_unwrapped.scene.terrain.terrain_types[done_env_ids]
sfx = f"track_l{col}"
metrics[f"total_score_{sfx}"] = ...

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.__init__ / BaseScorer.on_step / BaseScorer.get_metrics
}
```

### 34. Track 记录 max_segment_reached，可用于判断卡在哪一段

```text
{
关键内容:
BaseScorer 会根据 root_pos.x 计算当前赛道段索引，并记录 episode 内最大达到段 max_segment_reached。

解读:
虽然最终 score_detail 不按子地形段展开，但 max_segment_reached 能判断模型大概走到了第几段。Track 长度为 5 时，0~4 分别代表不同赛道段。

具体代码:
track_start_x = -self._track_total_x / 2.0
relative_x = pos_x - track_start_x
segment_idx = (relative_x / self._terrain_length_x).long()
self._max_segment_reached = torch.maximum(self._max_segment_reached, segment_indices)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._compute_segment_indices / _accumulate_per_step
}
```

### 35. standard 越界后停止累计质量指标，track 不受该门控影响

```text
{
关键内容:
standard 中 out_of_bounds 后 pose/energy/forward/step_count 受 in_bounds 门控；track 任务会把 in_bounds_f 强制置 1，使姿态、能耗、前进距离和步数按 actual_steps 统计。

解读:
Standard 越界会让后续质量指标停止增长，避免跨地形污染；Track 走出首段是正常行为，因此不阻断评分累计。标准任务中横向乱跑可能导致越界后不再涨 forward。

具体代码:
in_bounds = ~self._out_of_bounds
in_bounds_f = in_bounds.float()

if is_track_task:
    in_bounds_f = torch.ones_like(in_bounds_f)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step
}
```

### 36. Critic 观测额外拼接 goal_position_in_robot_frame

```text
{
关键内容:
CriticObservationProcess 的输出是 default_critic_obs + goal_position_in_robot_frame()，注释说明 goal position 维度为 4。

解读:
Critic 可能拥有比 policy 更多的目标信息。PPO 训练时 value function 能看到目标相对信息，能帮助学习；但推理时 actor 是否能看到 goal 要看 PolicyObservationProcess，不能只看 critic。

具体代码:
return self.concatenate_terms(
    self.default_observation(),
    self.goal_position_in_robot_frame(),
)

引用:
来源文件：agent_ppo/feature/critic_observation_process.py 或 agent_diy/feature/critic_observation_process.py
类：CriticObservationProcess
}
```

### 37. EnvMonitor 只是上报，不决定最终评分

```text
{
关键内容:
EnvMonitor 把 BaseScorer.flush() 的 metrics 上报到 Prometheus；use_prometheus=false 时进入静默模式，只打日志。

解读:
监控面板指标来自 BaseScorer，但最终评估 JSON 使用 BaseScorer lifetime metrics。监控上报失败通常不影响训练和最终评分。

具体代码:
scorer._on_flush = self._handle_metrics
self._scorer.on_step(...)
self._try_flush()

引用:
来源文件：tools/base_env/monitor.py
类：EnvMonitor
}
```

### 38. EnvMonitor 默认 60 秒 flush，一次 flush 会清空普通 metrics

```text
{
关键内容:
EnvMonitor 默认 flush_interval_sec=60；_try_flush 满足时间间隔后调用 scorer.flush()，flush 会 get_metrics 后 reset_metrics。

解读:
训练监控面板显示的是窗口统计，不是全生命周期统计。评估模式下 BaseScorer 另有 lifetime 缓冲区，因此最终 JSON 不会被 60 秒 flush 清空。

具体代码:
if now - self._last_flush_time >= self._flush_interval_sec:
    self._scorer.flush()
    self._last_flush_time = now

def flush(self):
    metrics = self.get_metrics()
    self._on_flush(metrics)
    self.reset_metrics()

引用:
来源文件：tools/base_env/monitor.py / tools/base_env/base_scorer.py
函数：EnvMonitor._try_flush / BaseScorer.flush
}
```

## 第二批：ObservationBridge / ObservationProcess / RewardBridge / VideoWriter

### 39. ObservationBridge 会整体覆盖 policy/critic observation group

```text
{
关键内容:
ObservationBridge 的默认策略是覆盖指定 observation group。它先深拷贝原 group 配置作为 source，然后把目标 group 替换成只有一个 custom_obs term 的新 group。

解读:
模型最终收到的观测不是 Isaac Lab 原始 group 直接输出，而是 ObservationProcess.process() 的返回值。也就是说，真正的 policy/critic 输入维度和内容，必须看 agent_xxx.feature.PolicyObservationProcess / CriticObservationProcess 的 process()。

具体代码:
self._source_group_cfgs[self._target_group] = copy.deepcopy(getattr(env_cfg.observations, self._target_group))
setattr(env_cfg.observations, self._target_group, self.build_group_cfg(self._target_group))

引用:
来源文件：tools/base_env/observation_bridge.py
类：ObservationBridge
函数：override_group_in_env_cfg
}
```

### 40. default_observation() 是被覆盖前的原始观测

```text
{
关键内容:
ObservationBridge 保存原始 group 后，会在 wrapper 中注入 _default_compute_observations 回调。ObservationProcess.default_observation() 调这个回调，拿到被覆盖前的 Isaac Lab 默认观测。

解读:
自定义观测不是必须从零开始写。最稳的做法通常是 default_observation() + 额外信息拼接，例如高度扫描、目标位置、速度等。若 process() 不调用 default_observation()，原始 proprio/height_scan/command 信息可能完全丢失。

具体代码:
self._observation_process._default_compute_observations = self._make_default_compute_callback(group_name, env)
return bound_method()

def default_observation(self):
    return self._default_compute_observations()

引用:
来源文件：tools/base_env/observation_bridge.py / tools/base_env/observation_process.py
函数：ObservationBridge._make_wrapper / ObservationProcess.default_observation
}
```

### 41. custom observation group 关闭了额外噪声、历史和二次干预

```text
{
关键内容:
ObservationBridge 构建的新 group 设置 concatenate_terms=True、enable_corruption=False、history_length=None、flatten_history_dim=True。

解读:
最终 custom_obs 外层不会再被 Isaac Lab group 级别加噪声或历史堆叠。若想要历史观测，必须在 ObservationProcess.process() 里自己拼；若想要噪声，需要确认 default_observation() 的原始 source group 是否已经包含噪声，或在自定义 process 里主动加。

具体代码:
group_cfg.concatenate_terms = True
group_cfg.enable_corruption = False
group_cfg.history_length = None
group_cfg.flatten_history_dim = True

引用:
来源文件：tools/base_env/observation_bridge.py
函数：ObservationBridge.build_group_cfg
}
```

### 42. ObservationProcess 在每次计算观测时延迟绑定 env

```text
{
关键内容:
ObservationBridge 的 wrapper 每次被 ObservationManager 调用时，都会把当前 env 绑定到 observation_process.env。

解读:
process() 里可以实时读取 env.scene["robot"]、传感器、terrain、goal_positions 等运行时状态。这是感知数据从仿真环境传到模型输入的关键入口。

具体代码:
def wrapper(env):
    self._observation_process.env = env
    self._observation_process._default_compute_observations = ...
    return bound_method()

引用:
来源文件：tools/base_env/observation_bridge.py
函数：ObservationBridge._make_wrapper
}
```

### 43. 默认 proprio 观测结构里 command 在第 6 到 8 维

```text
{
关键内容:
extract_obs_without_command 注释明确 Isaac Lab 默认 45 维结构为：[ang_vel(3), gravity(3), commands(3), joint_pos_rel(12), joint_vel_rel(12), last_action(12)]。

解读:
如果要做特征裁剪或模型结构改造，这个维度顺序很关键。commands 是速度命令，删除它会让策略不知道当前目标速度；保留它则策略能跟随平台采样命令。

具体代码:
if obs_dim == 45:
    return torch.concat(
        (
            obs[:, :6],
            obs[:, 9:],
        ),
        dim=1,
    )

引用:
来源文件：tools/base_env/observation_process.py
函数：extract_obs_without_command
}
```

### 44. height_scan 的真实计算方式是传感器高度减射线命中高度再减 offset

```text
{
关键内容:
height_scan(sensor_name="height_scanner", offset=0.5, clip_range=(-1.0, 5.0)) 从 RayCaster 读取 pos_w 和 ray_hits_w，计算 sensor_z - hit_z - offset，并裁剪。

解读:
height_scan 是地形感知的核心。数值越大通常表示脚下/前方更低，数值越小甚至负值可能表示前方障碍或高处。训练迷宫、楼梯、坡道时，策略能否利用这个网格比单纯 proprio 更重要。

具体代码:
sensor = sensors[sensor_name]
scan = sensor.data.pos_w[:, 2].unsqueeze(1) - sensor.data.ray_hits_w[..., 2] - offset
scan = scan.clip(clip_range[0], clip_range[1])

引用:
来源文件：tools/base_env/observation_process.py
函数：ObservationProcess.height_scan
}
```

### 45. goal_position_in_robot_frame 的维度由 Config.CURRENT.num_goal_obs 控制

```text
{
关键内容:
ObservationProcess 通过 KAIWU_ALGORITHM 动态导入 agent_{algo}.conf.conf.Config，然后读取 Config.CURRENT.num_goal_obs。

解读:
目标相关观测维度不是写死在 tools 里，而是跟当前算法和阶段配置绑定。改 stage 或切换 ppo/diy 时，要保证 num_goal_obs、模型输入维度、Policy/CriticObservationProcess 拼接内容一致。

具体代码:
algo = os.environ.get("KAIWU_ALGORITHM", "ppo")
module_name = f"agent_{algo}.conf.conf"
module = importlib.import_module(module_name)
return module.Config

num_goal_obs = Config.CURRENT.num_goal_obs

引用:
来源文件：tools/base_env/observation_process.py
函数：_get_algo_config / ObservationProcess._get_num_goal_obs
}
```

### 46. goal_positions 是平台维护的地形基础设施

```text
{
关键内容:
ensure_goal_positions_ready 会给 env 分配 goal_positions、goal_yaw，并初始化 TerrainExitManager。base_env.step 在 track 模式每步调用它和 update_goal_positions。

解读:
goal_positions 不只是观测项，它同时服务于 scorer、termination 和 reward。Track 如果 goal_positions 初始化失败，completion 判定就会坏；如果策略观测没包含 goal，也只是 actor 看不到目标，不代表平台没有目标数据。

具体代码:
env.goal_positions = torch.zeros(num_envs, 3, device=device)
env.goal_yaw = torch.zeros(num_envs, device=device)
manager = TerrainExitManager(device=device)
env._terrain_exit_manager = manager

引用:
来源文件：tools/base_env/observation_process.py
函数：ensure_goal_positions_ready
}
```

### 47. 不同地形模式的 goal 语义不同

```text
{
关键内容:
plane 模式 goal 是机器人 yaw 前方 10m；track 模式 goal 是对应并行赛道终点段出口；standard grid 模式 goal 按 (terrain_level, terrain_type) 查询当前块出口。

解读:
这解释了导航任务的目标来源。Track 的终点不是当前段局部目标，而是 finish-row exit；Standard 的目标跟当前地形块类型和难度有关。策略如果使用 goal 观测，要明确它代表的是哪个出口。

具体代码:
if terrain.cfg.terrain_type == "plane":
    env.goal_positions[:, 0] = robot_pos[:, 0] + distance * torch.cos(robot_yaw)
elif manager.is_track_mode:
    goal_pos, goal_yaw = manager.get_track_goal_positions(robot_pos_x, terrain_types)
else:
    goal_pos, goal_yaw = manager.get_goal_positions(terrain_levels, terrain_types)

引用:
来源文件：tools/base_env/observation_process.py
函数：update_goal_positions
}
```

### 48. Track goal 由 terrain_types 决定并行赛道列

```text
{
关键内容:
track 模式 update_goal_positions 使用 robot_pos_x 和 terrain.terrain_types 调用 get_track_goal_positions。

解读:
terrain_types 在 track 中本质上是并行赛道/难度列索引。goal 会随机器人所在赛道列变化，而不是所有 env 共用一个终点。

具体代码:
robot_pos_x = robot.data.root_pos_w[:, 0]
terrain_types = terrain.terrain_types
goal_pos, goal_yaw = manager.get_track_goal_positions(robot_pos_x, terrain_types)

引用:
来源文件：tools/base_env/observation_process.py
函数：update_goal_positions
}
```

### 49. 平台自带 PolicyObservationProcess 示例只返回 base_lin_vel

```text
{
关键内容:
这份 PolicyObservationProcess.process() 示例中 default_observation() 被注释掉，实际只拼接 self.base_lin_vel()。

解读:
如果这是实际生效的 agent_xxx.feature.PolicyObservationProcess，那么 actor 只能看到 3 维 base linear velocity，完全看不到地形、命令、关节状态和目标，基本无法完成比赛。实际提交前必须确认 agent_ppo/agent_diy 下生效的 PolicyObservationProcess 是否保留了必要感知。

具体代码:
return self.concatenate_terms(
    # self.default_observation(),
    self.base_lin_vel(),
)

引用:
来源文件：tools/base_env/policy_observation_process.py 或用户当前复制的 PolicyObservationProcess
类：PolicyObservationProcess
}
```

### 50. RewardBridge 会用 TOML 完全驱动 reward 注册

```text
{
关键内容:
RewardBridge.from_configs 遍历 reward_configs，对每个 [rewards.xxx] 查找 RewardProcess._reward_xxx 方法，注册 weight 和 params。

解读:
训练反馈信号的来源是 TOML，不是 RewardProcess 里所有方法自动生效。写在 RewardProcess 但 TOML 没配的 reward 不会参与训练；TOML 写了但方法不存在会直接报错。

具体代码:
custom_method_name = f"{cls.REWARD_PREFIX}{term_name}"
custom_method = getattr(reward_process, custom_method_name, None)

bridge._terms[term_name] = {
    "source": "custom",
    "method": custom_method,
    "weight": float(weight),
    "params": toml_params,
}

引用:
来源文件：tools/base_env/reward_bridge.py
类：RewardBridge
函数：from_configs
}
```

### 51. RewardBridge 注册时会全量替换 env_cfg.rewards

```text
{
关键内容:
register_to_env_cfg 构建 RewardsCfg 后执行 env_cfg.rewards = RewardsCfg()，不是和默认奖励合并。

解读:
一旦 TOML 有 rewards 配置，最终训练 reward 就是 TOML 中列出的那些项。不要以为 Isaac Lab 默认奖励还在；缺失关键奖励会直接改变训练目标。

具体代码:
def register_to_env_cfg(self, env_cfg):
    RewardsCfg = self.build_rewards_cfg()
    env_cfg.rewards = RewardsCfg()

引用:
来源文件：tools/base_env/reward_bridge.py
函数：RewardBridge.register_to_env_cfg
}
```

### 52. reward 参数从 TOML 透传，缺必需参数会构建期报错

```text
{
关键内容:
RewardBridge 会 inspect _reward_xxx 方法签名。方法有显式参数或 **kwargs 时，TOML params 会按名称透传；缺少无默认值的必需参数会立即 raise ValueError。

解读:
这能帮你判断“为什么 reward 没启动/训练启动失败”。如果想让 reward 可配置，就把参数写进方法签名并在 TOML 的 [rewards.xxx.params] 里给值。

具体代码:
all_method_params, required_method_params, has_var_keyword = self._inspect_custom_method(bound_method)
self._validate_custom_params(term_name, bound_method, toml_params, required_method_params)

引用:
来源文件：tools/base_env/reward_bridge.py
函数：RewardBridge._make_wrapper / _validate_custom_params
}
```

### 53. reward 计算时也会延迟绑定真实 env

```text
{
关键内容:
RewardBridge wrapper 每次被 Isaac Lab RewardManager 调用时，都会执行 self._reward_process.env = env，再调用对应 _reward_xxx。

解读:
RewardProcess 里的 reward 可以实时读取 robot、contact sensor、height_scanner、termination_manager、goal_positions 等环境状态。这是训练反馈信号从仿真环境回传到 PPO 的关键链路。

具体代码:
def wrapper(env, ...):
    self._reward_process.env = env
    return _bound_method(...)

引用:
来源文件：tools/base_env/reward_bridge.py
函数：RewardBridge._make_wrapper
}
```

### 54. reward 权重优先级：TOML 最高

```text
{
关键内容:
RewardBridge 注释说明权重优先级为 TOML config weight > reward_weights dict > default 1.0。配置驱动模式实际使用 TOML weight。

解读:
调 reward 权重主要改 TOML。代码里默认值或旧 reward_weights 不会覆盖 TOML。训练曲线异常时，先检查 train_env_conf_xxx.toml 的 [rewards.*].weight。

具体代码:
weight = term_cfg.get("weight", 1.0)
"weight": float(weight)

引用:
来源文件：tools/base_env/reward_bridge.py
类：RewardBridge
函数：from_configs
}
```

### 55. RewardBridge 有 legacy scan，但推荐路径是 config-driven

```text
{
关键内容:
RewardBridge 支持 legacy scan 自动扫描全部 _reward_*，也支持 from_configs 按 TOML 注册。base_env 中使用的是 create_bridge_from_configs/reward_configs 这条配置驱动路径。

解读:
不要被 RewardProcess 里一堆 _reward_* 方法误导。比赛训练实际启用哪些 reward，以 TOML 为准。额外写方法只是提供可选项。

具体代码:
reward_configs = parse_reward_configs(usr_conf)
bridge = reward_process.create_bridge_from_configs(reward_configs)
bridge.register_to_env_cfg(env_cfg)

引用:
来源文件：tools/base_env/base_env.py / tools/base_env/reward_bridge.py
函数：Robot._create_env / RewardBridge.from_configs
}
```

### 56. 视频文件名可以携带 env 对应地形/level 标签

```text
{
关键内容:
TiledCameraVideoWriter 支持 env_terrain_names，文件名格式可变为 env_{id:03d}_{terrain}.mp4，例如 env_004_L1_pyramid_slope.mp4。

解读:
评估视频可以直接按文件名定位地形和难度。复盘失败时，不需要只看 env_id，可以结合文件名判断是哪个 level、哪类地形失败。

具体代码:
filename = f"env_{env_id:03d}.mp4"
if names is not None and 0 <= env_id < len(names):
    terrain = self._sanitize_terrain_name(str(names[env_id]))
    filename = f"env_{env_id:03d}_{terrain}.mp4"

引用:
来源文件：tools/base_env/video_writer.py
类：TiledCameraVideoWriter
函数：_build_video_path
}
```

### 57. 视频写入支持 skip_mask，某些 env 可以不写帧

```text
{
关键内容:
write_frames(rgb_tensor, skip_mask) 中，如果 skip_mask[env_id]=True，则该 env 当前帧不会写入视频。

解读:
Standard 越界、评估首次 done 后，视频可能停止写帧。看到空视频或很短视频时，不一定是渲染坏了，可能是该 env 首步终止或被 skip_mask 屏蔽。

具体代码:
for env_id in range(min(num_envs, self._num_envs)):
    if skip_mask is not None and skip_mask[env_id]:
        continue
    self._writers[env_id].write(frame_bgr)

引用:
来源文件：tools/base_env/video_writer.py
函数：TiledCameraVideoWriter.write_frames
}
```

### 58. 空壳 MP4 会被保留用于定位异常

```text
{
关键内容:
close() 会检查 frame_count，如果某个 MP4 0 帧，会打 warning，但不会删除文件。

解读:
0 帧 MP4 是重要问题现场：可能是整段 episode 被越界 skip，或首步即终止。要结合 base_env 的 [eval term-debug] 日志看具体触发了哪个 termination term。

具体代码:
if frame_cnt == 0 and os.path.isfile(path):
    empty_files.append(path)

self._logger.warning(
    f"检测到 {len(empty_files)} 个空壳 mp4（0 帧），已保留用于问题定位。"
)

引用:
来源文件：tools/base_env/video_writer.py
函数：TiledCameraVideoWriter.close
}
```

### 59. 视频编码优先使用 H.264，并要求偶数分辨率

```text
{
关键内容:
VideoWriter 编码候选依次为 avc1、H264、h264、mp4v。若宽高不是偶数，会裁剪到偶数尺寸。

解读:
这只影响视频兼容性，不影响得分。若视频播放黑屏，可能和编码器候选或本地播放器有关；评估 JSON 分数仍应以 result 文件为准。

具体代码:
codec_candidates = ["avc1", "H264", "h264", "mp4v"]
if (w % 2) or (h % 2):
    w = w - (w % 2)
    h = h - (h % 2)

引用:
来源文件：tools/base_env/video_writer.py
函数：TiledCameraVideoWriter.write_frames
}
```

### 60. 感知到反馈的完整链路

```text
{
关键内容:
感知链路：Isaac Lab env.scene / sensors / terrain → ObservationBridge wrapper 绑定 env → ObservationProcess.process() 读取 default_observation、robot、height_scanner、goal_positions → 输出 policy/critic custom_obs → agent.predict 输出 action。反馈链路：action → env.step → RewardManager 调 RewardBridge wrapper → RewardProcess._reward_xxx 读取 env 状态 → reward 给 PPO；同时 BaseScorer.on_step 独立累计最终 score。

解读:
比赛里有两套反馈：训练 reward 反馈用于学策略，最终 score 反馈用于平台评分。观测决定策略能“看见”什么，reward 决定训练时鼓励什么，BaseScorer 决定最后分数怎么算。调参时要同时检查这三条链，不能只盯 reward。

具体代码:
ObservationBridge.wrapper(env) -> ObservationProcess.process()
RewardBridge.wrapper(env) -> RewardProcess._reward_xxx()
BaseScorer.on_step(env_unwrapped, dones) -> total_score / pose_score / energy_score

引用:
来源文件：tools/base_env/observation_bridge.py
来源文件：tools/base_env/observation_process.py
来源文件：tools/base_env/reward_bridge.py
来源文件：tools/base_env/base_scorer.py
}
```

## 第三批补充：最终得分全链路与参数来源详解

### 61. 最终得分不是单步 reward，而是 episode 结束后的统计分

```text
{
关键内容:
平台最终分数由 BaseScorer 在 episode done 时计算。每步只做累计，真正生成 pose_score、energy_score、time_score、forward_score、total_score 是在 dones 中有 env 结束后。

解读:
训练 reward 是每步反馈，最终 score 是 episode 级反馈。一次 episode 中途姿态/能耗/距离不断累积，done 时除以步数求平均，再转换成 0~100 分。优化比赛成绩时，要关注整段 episode 的平均表现，而不是某一步的瞬时 reward。

具体代码:
self._accumulate_per_step(env_unwrapped)
if not dones.any():
    return

mean_pose_deviation = done_pose_accum[i].item() / accum_steps
pose_score = self._compute_posture_score(mean_pose_deviation)
mean_energy = done_energy_accum[i].item() / accum_steps
energy_score = self._compute_energy_score(mean_energy)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.on_step
}
```

### 62. 最终评分链路的完整执行顺序

```text
{
关键内容:
完整顺序是：Robot.reset 读取 usr_conf 并创建 env_cfg → 初始化 BaseScorer → Robot.step 保存 step 前快照 → env.step(actions) 推进仿真 → track 更新 goal_positions → 解析 terminated/truncated → eval 单次生命过滤 → EnvMonitor/BaseScorer.on_step 累积分数 → done 时写入 episode 分数 → 评估结束 _build_end_info 写 result JSON。

解读:
这说明最终得分是从平台环境包装层产生的，不是 agent 直接上报。你能影响的是 action、训练出的策略、TOML 配置和 reward；最终统计口径由 BaseScorer 固定。

具体代码:
Robot.reset:
    apply_usr_conf_to_env_cfg(...)
    self._create_env(...)
    self._init_monitor()

Robot.step:
    pre_step_episode_lengths = env_unwrapped.episode_length_buf.clone()
    obs_raw, rewards, dones, extras = self.env.step(actions)
    self._env_monitor.on_step(env_unwrapped, dones_for_scorer, ...)

Robot._build_end_info:
    metrics = self._scorer.get_lifetime_metrics()

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.reset / Robot.step / Robot._build_end_info
}
```

### 63. max_episode_length 的来源和含义

```text
{
关键内容:
max_episode_length 优先由 [env].episode_length_s 换算，step_dt 固定按 0.02 秒计算，即 max_episode_length = episode_length_s / 0.02。

解读:
max_episode_length 是 time_score 的分母，也是评估最长运行步数。episode_length_s 越大，完成同样步数的 time_score 越高；但正式评估配置由平台决定，训练时应模拟真实 episode_length_s。

具体代码:
episode_length_s = env_section.get("episode_length_s")
if episode_length_s is not None:
    step_dt = 0.02
    self.max_episode_length = int(float(episode_length_s) / step_dt)

引用:
来源文件：tools/base_env/base_env.py
函数：Robot.reset
}
```

### 64. actual_steps 的来源和为什么要 step 前快照

```text
{
关键内容:
actual_steps 来自 env.step 前保存的 episode_length_buf + 1。因为 Isaac Lab 内部 done 后会 auto-reset，把 episode_length_buf 清零。

解读:
时间分数用的不是 reset 后读到的 0，而是真实 episode 结束时步数。这里避免了 auto-reset 污染评分。对比赛来说，越快 done 且成功，time_score 越高。

具体代码:
pre_step_episode_lengths = env_unwrapped.episode_length_buf.clone()

if pre_step_episode_lengths is not None:
    episode_lengths = pre_step_episode_lengths[done_env_ids] + 1
else:
    episode_lengths = env_unwrapped.episode_length_buf[done_env_ids]

引用:
来源文件：tools/base_env/base_env.py / tools/base_env/base_scorer.py
函数：Robot.step / BaseScorer.on_step
}
```

### 65. time_score 的参数来源和意义

```text
{
关键内容:
time_score = max(0, 1 - actual_steps / max_steps) * 100。Track 用 episode 结束步数 actual_steps；Standard 用首次走穿步数 traverse_step，未走穿时 time_score=0。

解读:
Track 的时间分只有完成目标后才有意义，因为 total_score 外面乘 completion_coeff。Standard 的时间分只看首次达到走穿阈值的时刻，不看之后是否继续跑满。

具体代码:
def _compute_time_score(actual_steps, max_steps):
    return max(0.0, 1.0 - actual_steps / max_steps) * 100.0

if self._task_type == "standard":
    if self._traversed[env_idx]:
        time_score = self._compute_time_score(traverse_steps, self._max_episode_length)
    else:
        time_score = 0.0
else:
    time_score = self._compute_time_score(steps, self._max_episode_length)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._compute_time_score / BaseScorer.on_step
}
```

### 66. root_pos_w 的来源、物理含义和参与哪些分数

```text
{
关键内容:
root_pos_w 来自 Isaac Lab robot asset 的 root 世界坐标，shape 为 (num_envs, 3)。它参与 standard 的出生点距离、越界判断、track 段索引、plane goal 生成、视频相机跟随等逻辑。

解读:
root_pos_w 是平台判断“狗在哪里”的核心状态。Standard 的 forward_score 不是脚步距离，而是 root 在 XY 平面离 episode 起点的最远距离。Track 的 max_segment_reached 也由 root_pos_w[:, 0] 计算。

具体代码:
robot = env_unwrapped.scene["robot"]
root_pos = robot.data.root_pos_w

delta_xy = root_pos[:, :2] - self._episode_start_xy
dist_from_start = torch.norm(delta_xy, dim=-1)

segment_indices = self._compute_segment_indices(root_pos[:, 0])

引用:
来源文件：tools/base_env/base_scorer.py / tools/base_env/observation_process.py
函数：BaseScorer._accumulate_per_step / update_goal_positions
}
```

### 67. root_quat_w 的来源、物理含义和姿态分计算

```text
{
关键内容:
root_quat_w 来自 robot.data.root_quat_w，格式是 WXYZ 四元数。BaseScorer 从它解算 roll 和 pitch，姿态偏移 pose_deviation = |roll| + |pitch|。

解读:
pose_score 不看 yaw，也不直接看 projected_gravity_b；它看机身 roll/pitch 的绝对值，单位是弧度。身体左右翻滚、前后俯仰都会扣姿态分；原地转向 yaw 本身不扣姿态分。

具体代码:
w, x, y, z = quat[:, 0], quat[:, 1], quat[:, 2], quat[:, 3]
roll = torch.atan2(2.0 * (w * x + y * z), 1.0 - 2.0 * (x * x + y * y))
pitch = torch.asin(torch.clamp(2.0 * (w * y - z * x), -1.0, 1.0))
pose_deviation = torch.abs(roll) + torch.abs(pitch)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._quat_to_roll_pitch / BaseScorer._accumulate_per_step
}
```

### 68. pose_score 的完整参数解释

```text
{
关键内容:
pose_score = 100 * exp(-5 * mean_deviation)，mean_deviation = sum_over_steps(|roll| + |pitch|) / accum_steps。

解读:
mean_deviation 是整段 episode 平均机身倾斜程度，越接近 0 越好。因为指数系数是 5，姿态分衰减很快。例如平均偏移 0.1 rad 时，pose_score 约 60.65；平均偏移 0.2 rad 时，约 36.79。

具体代码:
self._pose_accum += pose_deviation * in_bounds_f
mean_pose_deviation = done_pose_accum[i].item() / accum_steps
return 100.0 * math.exp(-5.0 * mean_deviation)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step / BaseScorer._compute_posture_score
}
```

### 69. joint_vel 的来源、物理含义和能耗分关系

```text
{
关键内容:
joint_vel 来自 robot.data.joint_vel，表示各关节角速度，通常单位 rad/s。BaseScorer 对所有关节取 abs(joint_vel) 参与机械功率估计。

解读:
关节动得越快，在相同 torque 下能耗越高。高频抖腿、快速弹跳、落地后关节高速回摆都会提高 mean_energy，降低 energy_score。

具体代码:
joint_vel = robot.data.joint_vel
energy_per_env = torch.sum(torch.abs(joint_vel) * torch.abs(applied_torque), dim=-1)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step
}
```

### 70. applied_torque 的来源、物理含义和能耗分关系

```text
{
关键内容:
applied_torque 来自 robot.data.applied_torque，表示仿真中实际施加到关节的力矩。BaseScorer 使用 abs(applied_torque) 与 abs(joint_vel) 相乘。

解读:
这不是 actor 输出 action 本身，而是经过 PD 控制、actuator、物理约束后的实际关节力矩。动作幅度大、身体冲击大、强行纠姿都会让 torque 上升，从而降低 energy_score。

具体代码:
applied_torque = robot.data.applied_torque
energy_per_env = torch.sum(torch.abs(joint_vel) * torch.abs(applied_torque), dim=-1)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step
}
```

### 71. energy_score 的完整参数解释

```text
{
关键内容:
energy_score = 100 * exp(-0.01 * mean_energy)，mean_energy = sum_over_steps(sum(|joint_vel| * |applied_torque|)) / accum_steps。

解读:
mean_energy 是每步全关节机械功率近似值的 episode 平均。它惩罚的是“高速度 × 高力矩”的组合；慢速大 torque 或高速小 torque 都比高速大 torque 好。跳跃如果带来大扭矩和高速关节运动，会明显扣能耗分。

具体代码:
self._energy_accum += energy_per_env * in_bounds_f
mean_energy = done_energy_accum[i].item() / accum_steps
return 100.0 * math.exp(-0.01 * mean_energy)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step / BaseScorer._compute_energy_score
}
```

### 72. accum_steps 的来源和为什么 Track/Standard 不一样

```text
{
关键内容:
accum_steps = max(done_step_count, 1)。Track 中 step_count 每步无条件 +1；Standard 中 step_count 只在 in_bounds 时 +1。

解读:
Track 的姿态/能耗平均按真实 episode 步数算。Standard 越界后不再累计质量指标，也不增加 step_count，避免跨地形数据污染当前块评分。

具体代码:
if is_track_task:
    self._step_count += 1
else:
    self._step_count += in_bounds.long()

accum_steps = max(done_step_count[i].item(), 1)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step / BaseScorer.on_step
}
```

### 73. forward_score 的完整参数解释

```text
{
关键内容:
forward_score = min(d_forward / L_forward_score, 1.0) * 100。Standard 中 d_forward 是 episode 内 max(||current_xy - episode_start_xy||_2)，L_forward_score = terrain_length_x / 2。

解读:
forward_score 表示离出生点最远走出了半块地形的多少比例。它不累计路径长度，所以来回抖动不能刷距离；它也不看方向，所以理论上横向远离起点也会增加，但越界和地形结构会限制。

具体代码:
dist_from_start = torch.norm(delta_xy, dim=-1)
self._forward_accum = torch.maximum(self._forward_accum, gated_dist)

def _forward_score_length(self):
    if self._task_type == "standard":
        return self._terrain_length_x / 2.0

return min(d_forward / l_terrain, 1.0) * 100.0

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step / _forward_score_length / _compute_forward_score
}
```

### 74. terrain_length_x / terrain_length_y 的来源和意义

```text
{
关键内容:
terrain_length_x 和 terrain_length_y 来自 terrain_generator_cfg.size，读取失败时默认 8.0。Standard 用 size_x/2 做 forward_score 满分长度和走穿阈值；越界检测用 size_x/2、size_y/2。

解读:
Standard 不是按整块长度计分，而是按半块长度。默认 size_x=8 时，forward_score 满分距离约 4m，走穿阈值约 3.9m。

具体代码:
self._terrain_length_x = float(terrain_gen_cfg.size[0])
self._terrain_length_y = float(terrain_gen_cfg.size[1])

return self._terrain_length_x / 2.0
return self._terrain_length_x / 2.0 - 0.1

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.__init__ / _forward_score_length / _traversal_threshold
}
```

### 75. Standard 走穿判定参数 traverse_step 的来源

```text
{
关键内容:
Standard 首次满足 forward_accum >= terrain_length_x / 2 - 0.1 时，记录当前 _physics_step_count 为 traverse_step。

解读:
traverse_step 是 time_score 的实际分子。它不是 episode 最终步数，而是首次接近半块边界的步数。训练时鼓励尽快到达阈值比后续继续跑更关键。

具体代码:
threshold = self._traversal_threshold()
just_traversed = not_yet & (self._forward_accum >= threshold)
self._traverse_step[just_traversed] = self._physics_step_count[just_traversed]
self._traversed[just_traversed] = True

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._accumulate_per_step
}
```

### 76. completion_coeff 的来源和意义

```text
{
关键内容:
Track 的 completion_coeff 只由 is_goal_reached 决定，is_goal_reached 来自 termination_manager.get_term("goal_reached")。

解读:
Track 总分外面乘 completion_coeff。只要 goal_reached=False，total_score=0；即使时间短、姿态稳、能耗低，也不会有总分。Track 第一目标是进入 goal_reached 判定范围。

具体代码:
term_flags = term_mgr.get_term("goal_reached")
return term_flags[done_env_ids].bool()

completion_coeff = 1.0 if is_goal_reached else 0.0

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer._get_goal_reached / BaseScorer.on_step
}
```

### 77. goal_reached 的前置感知来源是 goal_positions

```text
{
关键内容:
Track 每步由 update_goal_positions 维护 env.goal_positions。goal_positions 来自 TerrainExitManager，根据赛道/地形出口计算。

解读:
goal_reached 的判定目标不是模型自己定义的，而是平台从地形出口管理器生成的。策略若要导航，policy observation 最好包含目标相对信息；否则只能靠地形高度和经验盲走。

具体代码:
if ensure_goal_positions_ready(env_unwrapped):
    update_goal_positions(env_unwrapped)

goal_pos, goal_yaw = manager.get_track_goal_positions(robot_pos_x, terrain_types)
env.goal_positions.copy_(goal_pos)

引用:
来源文件：tools/base_env/base_env.py / tools/base_env/observation_process.py
函数：Robot.step / update_goal_positions
}
```

### 78. terrain_levels 的来源和意义

```text
{
关键内容:
terrain_levels 来自 env_unwrapped.scene.terrain.terrain_levels。Robot.step 在 env.step 前拍快照，BaseScorer 用它做 Standard 的 (terrain, level) 分组。

解读:
terrain_levels 是难度行索引。Standard 的 export_score_detail 会展开到 terrain_type_level{lv}，方便定位哪个难度失败。训练监控中看各 level 得分比看总均值更有指导意义。

具体代码:
pre_step_terrain_levels = terrain.terrain_levels.clone()
done_levels = pre_step_terrain_levels[done_env_ids]
sfx = f"{terrain_name}_l{lv}"

引用:
来源文件：tools/base_env/base_env.py / tools/base_env/base_scorer.py
函数：Robot.step / BaseScorer.on_step / BaseScorer.get_metrics
}
```

### 79. terrain_types 的来源和意义

```text
{
关键内容:
terrain_types 来自 env_unwrapped.scene.terrain.terrain_types。Standard 中它代表地形列，需要通过 proportion 映射到 sub_terrain 名称；Track 中它直接作为难度列/并行赛道列。

解读:
同一个字段在两种任务语义不同。Standard 用它区分 slope/stairs/maze 等子地形；Track 用它区分 track_l0、track_l1 等难度档。

具体代码:
self._env_terrain_name_idx = col_to_sub_idx_tensor[terrain.terrain_types.long()]
done_cols = env_unwrapped.scene.terrain.terrain_types[done_env_ids]

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.__init__ / BaseScorer.on_step
}
```

### 80. reward_sums 的来源和它与 score 的区别

```text
{
关键内容:
BaseScorer 会从 env.extras["log"] 中读取 "Episode_Reward/{term_name}"，作为 reward_{term_name} 指标上报，但不参与 total_score 计算。

解读:
reward_sums 是训练 reward 的 episode 统计，能帮助调参和排查，但它不是最终评分。比如 reward_energy 很差可能预示 energy_score 差，但 total_score 不直接加 reward_energy。

具体代码:
log_extras = getattr(env_unwrapped, "extras", {}).get("log", {})
if key.startswith("Episode_Reward/"):
    term_name = key[len(prefix):]
    reward_sums[term_name] = float(val)

metrics[f"reward_{term_name}_{name}"] = _safe_mean(vals)

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.on_step / BaseScorer.get_metrics
}
```

### 81. action 如何影响最终 energy_score

```text
{
关键内容:
agent 输出 action，经环境 action pipeline 和 actuator/PD 控制转成实际 applied_torque；BaseScorer 读取 applied_torque 与 joint_vel 计算能耗。

解读:
actor action 不是直接进入评分，但它通过关节目标、力矩和速度间接影响 energy_score。动作过大、变化太快、落地冲击大，都会让 applied_torque 或 joint_vel 上升。

具体代码:
训练侧:
command_actions = torch.clip(joint_actions, -6.0, 6.0)
data = env.step(command_actions)

评分侧:
joint_vel = robot.data.joint_vel
applied_torque = robot.data.applied_torque
energy_per_env = torch.sum(torch.abs(joint_vel) * torch.abs(applied_torque), dim=-1)

引用:
来源文件：agent_ppo/workflow/train_workflow.py / tools/base_env/base_scorer.py
函数：run_episodes_ / BaseScorer._accumulate_per_step
}
```

### 82. height_scan 如何进入模型感知，但不直接进入最终分数

```text
{
关键内容:
height_scan 从 RayCaster 传感器读取地形高度信息，可由 ObservationProcess 拼进 policy/critic observation。但 BaseScorer 计算 pose/energy/time/forward 不直接读取 height_scan。

解读:
height_scan 是策略导航和越障的感知输入，不是评分项。它的价值是让模型提前感知楼梯、坡、墙、迷宫，从而做出能完成 goal、姿态稳、能耗低的动作。

具体代码:
scan = sensor.data.pos_w[:, 2].unsqueeze(1) - sensor.data.ray_hits_w[..., 2] - offset
return self.concatenate_terms(self.default_observation(), self.height_scan(), ...)

引用:
来源文件：tools/base_env/observation_process.py
函数：ObservationProcess.height_scan
}
```

### 83. command 如何进入模型感知，但不直接进入最终分数

```text
{
关键内容:
Isaac Lab 默认 policy 观测中 commands 位于 45 维 proprio 的第 6~8 维，表示 base_velocity 命令。BaseScorer 最终评分不直接读取 command。

解读:
command 是训练时速度跟踪 reward 的目标输入。Standard/Track 最终分看完成、时间、姿态、能耗和距离；command 只是帮助训练出可控运动。如果最终策略过度追踪随机 command 而不朝评分目标走，可能 reward 高但 score 低。

具体代码:
默认观测结构:
[ang_vel(3), gravity(3), commands(3), joint_pos_rel(12), joint_vel_rel(12), last_action(12)]

引用:
来源文件：tools/base_env/observation_process.py
函数：extract_obs_without_command
}
```

### 84. policy observation 与 critic observation 在比赛中的角色

```text
{
关键内容:
policy observation 是 actor 推理时真正可见的信息；critic observation 只用于训练 value function。CriticObservationProcess 可以拼 goal_position_in_robot_frame，但这不代表 actor 推理时也能看到 goal。

解读:
如果 policy 不包含目标信息，Track 策略可能无法主动导航；critic 有目标只能帮助训练估值，不能在部署时替 actor 看路。检查比赛模型时必须分别核对 policy 和 critic 的 process()。

具体代码:
Critic:
return self.concatenate_terms(
    self.default_observation(),
    self.goal_position_in_robot_frame(),
)

Policy 示例:
return self.concatenate_terms(
    self.base_lin_vel(),
)

引用:
来源文件：agent_ppo/feature/critic_observation_process.py
来源文件：tools/base_env/policy_observation_process.py
}
```

### 85. result JSON 中各字段的具体意义

```text
{
关键内容:
end_info 顶层字段包括 total_score、pose_score、energy_score、episode_count、time_score；standard 额外有 forward_score；track 额外有 completion_coeff。score_detail 是分组均值，export_score_detail 是导出明细。

解读:
最终排名或面板通常读的是 end_info。total_score 是 episode total_score 的均值；pose/energy/time/forward 也是对应子分数均值；completion_coeff 是 completed / episode_count。

具体代码:
end_info = {
    "total_score": round(metrics.get("total_score")),
    "pose_score": round(metrics.get("pose_score")),
    "energy_score": round(metrics.get("energy_score")),
    "episode_count": total_episode_count,
    "time_score": round(metrics.get("step_score")),
}

if self._task_type == "standard":
    end_info["forward_score"] = ...
if self._task_type == "track":
    end_info["completion_coeff"] = completed / total_episode_count

引用:
来源文件：tools/base_env/base_env.py
函数：Robot._build_end_info
}
```

### 86. 各分数的比赛优化优先级

```text
{
关键内容:
Track：先 completion_coeff，再 time_score，再 pose_score/energy_score。Standard：先 forward_score 和是否 traversed，再 time_score，最后 pose/energy 做质量优化。

解读:
Track 没完成 total_score 直接 0，所以稳定到终点比省电重要。Standard 没走穿仍有 forward/pose/energy 部分分，但 time_score=0，所以要尽快达到半块阈值。跳跃策略只有在不显著拉高 mean_energy 和 mean_pose_deviation 的情况下才可能划算。

具体代码:
Track:
total_score = completion_coeff * (0.4*time + 0.4*pose + 0.2*energy)

Standard:
total_score = 0.4*forward + 0.2*time + 0.2*energy + 0.2*pose

引用:
来源文件：tools/base_env/base_scorer.py
函数：BaseScorer.on_step
}
```

## 第四批：eval / exam 工作流与评估配置来源

### 87. 评估配置由 base + eval_env_conf 深度合并得到

```text
{
关键内容:
评估 workflow 先加载 tools/conf/base/eval_env_base.toml，再加载 tools/eval/conf/eval_env_conf.toml，并用 user config 覆盖 base config 做 deep merge。

解读:
正式评估不是只看 eval_env_conf.toml。模型维度、基础环境参数可能来自 eval_env_base.toml，用户评估配置只覆盖其中一部分。排查评估行为时必须同时看这两个文件的合并结果。

具体代码:
_EVAL_CONF_FILE = "tools/eval/conf/eval_env_conf.toml"
_EVAL_BASE_FILE = "tools/conf/base/eval_env_base.toml"

base_config = toml.load(_EVAL_BASE_FILE)
user_config = toml.load(_EVAL_CONF_FILE)
config = _deep_merge(base_config, user_config)

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：_load_eval_conf / _deep_merge
}
```

### 88. eval_env_conf.toml 的配置会覆盖 base 配置

```text
{
关键内容:
_deep_merge(base, override) 中 override 优先。相同 key 下如果都是 dict，就递归合并；否则直接用 override 的值。

解读:
如果 eval_env_conf.toml 写了某个字段，它会覆盖 base 中对应字段。想知道最终评估参数，需要看合并后的打印日志，而不是单独看某一个 TOML。

具体代码:
if key in merged and isinstance(merged[key], dict) and isinstance(value, dict):
    merged[key] = _deep_merge(merged[key], value)
else:
    merged[key] = value

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：_deep_merge
}
```

### 89. 评估前会用 is_eval=True 做配置校验

```text
{
关键内容:
run_episodes 加载配置后调用 check_usr_conf(usr_conf, is_eval=True)。校验失败会直接 raise Exception。

解读:
评估模式下可能有比训练更严格的配置约束，例如 level 列表、地形模式、eval-only 字段等。某些训练能跑的配置，评估可能被 validator 拦下。

具体代码:
from tools.train_env_conf_validate import check_usr_conf
valid, message = check_usr_conf(usr_conf, is_eval=True, logger=logger)
if not valid:
    raise Exception(message)

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 90. 评估会完整打印合并后的配置

```text
{
关键内容:
评估开始后会打印 "Evaluation Configuration"，逐 section 输出 usr_conf 的最终值。

解读:
这是确认正式跑分配置的最直接来源。比猜 TOML 合并结果可靠。建议每次评估都保存这段日志，用来对照 episode_length_s、terrain.mode、level、sub_terrains、domain_rand、noise、save_mp4 等关键字段。

具体代码:
logger.info("========== Evaluation Configuration ==========")
for section, values in usr_conf.items():
    logger.info(f"[{section}]")
    logger.info(f"  {key} = {val}")

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 91. 评估会显式设置 usr_conf["is_eval"] = True

```text
{
关键内容:
在 env.reset 前，workflow 会给 usr_conf 写入 is_eval=True。

解读:
这会触发 base_env 中的评估专用逻辑：terrain seed 透传、单次生命统计、视频保存、eval level list 放置、track 起点策略、lifetime scorer 等。训练和评估不是完全同一套 runtime 行为。

具体代码:
usr_conf["is_eval"] = True
data = env.reset(usr_conf)

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 92. 评估推理只给 actor policy obs，不给 critic_obs

```text
{
关键内容:
评估循环里调用 agent.exploit((obs))，注释明确 "Only provide obs to prevent privileged information leakage"。critic_obs 虽然 reset 返回，但评估不传给 exploit。

解读:
正式推理时 actor 只能使用 policy observation，不能使用 critic 的特权信息。训练时 critic 拼 goal 或特权状态不等于部署时 actor 能看到。比赛策略能力最终取决于 policy obs。

具体代码:
(obs, critic_obs) = data

with torch.inference_mode():
    # Only provide obs to prevent privileged information leakage
    act_data = agent.exploit((obs))
    actions = act_data[0].action

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 93. 评估使用 exploit，不走训练采样逻辑

```text
{
关键内容:
评估中每步调用 agent.exploit，而不是 agent.predict/learn。整个循环包在 torch.inference_mode() 下。

解读:
评估动作应是确定性或推理模式动作，具体取决于 Agent.exploit 的实现。训练时的随机采样、log_prob、value、entropy 都不参与正式评估。

具体代码:
with torch.inference_mode():
    act_data = agent.exploit((obs))
    actions = act_data[0].action

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 94. 评估最大循环步数由 episode_nums_per_terrain 和 episode_length_s 决定

```text
{
关键内容:
episode_nums_per_terrain 默认从 custom_parameters.episode_nums_per_terrain 读取，默认 1。max_episode_length = episode_length_s / 0.02。total_steps = episode_nums_per_terrain * max_episode_length + 1。

解读:
这只是评估循环的外层步数上限。base_env 的评估单次生命逻辑会在每个 env 首次 done 后停止统计它；如果所有 env 都 done，会提前 all_done。默认等价于给每个 env 最多跑一个完整 episode。

具体代码:
episode_nums_per_terrain = usr_conf.get("custom_parameters", {}).get("episode_nums_per_terrain", 1)
episode_length_s = usr_conf.get("env", {}).get("episode_length_s", 20.0)
step_dt = 0.02
max_episode_length = int(episode_length_s / step_dt)
total_steps = episode_nums_per_terrain * int(max_episode_length) + 1

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 95. all_done 由 env.step 的 infos 控制，触发后评估立即返回

```text
{
关键内容:
每步 env.step 后，workflow 读取 infos。如果 infos["all_done"] 为 True，就 break/return。

解读:
评估结束信号来自 base_env.Robot.step。Robot.step 会在所有 env 首次完成、所有 env terminated、或超过 max_episode_length 时写 result JSON 并设置 all_done。workflow 本身不计算分数，只等环境告诉它结束。

具体代码:
frame_no, obs, rewards, terminated, truncated, state = step_data
infos, privileged_obs = state
if "all_done" in infos and infos["all_done"]:
    logger.info("all done is OK, so return")
    break

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 96. 评估结束时一定会调用 env.close

```text
{
关键内容:
run_episodes 的 finally 中调用 env.close()。注释说明 result json 要在 close 前生成，因为 close 会清理 scorer。

解读:
真正的 result JSON 通常在 Robot.step 检测 all_done 时生成；close 会最终 flush monitor、关闭视频写入器、清理 env/scorer。如果评估没有进入 all_done 生成 JSON，close 后 scorer 被清掉，结果可能异常。

具体代码:
finally:
    # Generate result json before close (scorer is cleared in close)
    env.close()

引用:
来源文件：tools/eval/workflow/eval_workflow.py
函数：run_episodes
}
```

### 97. exam_workflow 当前只是 eval_workflow 的薄包装

```text
{
关键内容:
exam_workflow.py 直接 import tools.eval.workflow.eval_workflow，并在 workflow 中调用 eval_workflow.workflow。

解读:
在这份可见代码里，考试 workflow 和评估 workflow 没有额外逻辑差异。真正差异更可能来自平台下发的 eval_env_conf、模型池、随机 seed、比赛运行参数，而不是 exam_workflow 本身。

具体代码:
from tools.eval.workflow.eval_workflow import eval_workflow

def workflow(*kargs, **kwargs):
    eval_workflow.workflow(*kargs, **kwargs)

引用:
来源文件：tools/eval/workflow/exam_workflow.py
函数：workflow
}
```

### 98. 评估配置里的 task_type 变量可能不是最终评分任务类型

```text
{
关键内容:
eval_workflow 中读取 task_type = usr_conf.get("env", {}).get("task", "standard")，但后续没有使用。base_env 实际评分 task_type 来自 terrain_section.get("mode", "track")。

解读:
判断 standard/track 不要看 eval_workflow 里的 task_type 变量，它只是未使用变量。真正影响 BaseScorer 分支的是 [terrain].mode。

具体代码:
eval_workflow:
task_type = usr_conf.get("env", {}).get("task", "standard")

base_env:
self._task_type = terrain_section.get("mode", "track").lower()

引用:
来源文件：tools/eval/workflow/eval_workflow.py
来源文件：tools/base_env/base_env.py
函数：run_episodes / Robot.reset
}
```

### 99. project_default 表明评估是 remote predict 模式

```text
{
关键内容:
project_default 配置 run_mode="eval"，predict_local_or_remote="remote"，aisrv_connect_to_kaiwu_env_count=1，并关闭模型同步 need_to_sync=false。

解读:
平台评估走远程预测服务链路，不是本地训练循环。模型同步关闭意味着评估时使用已指定/已加载的模型，不会像训练那样持续同步新 checkpoint。

具体代码:
[project_default]
run_mode = "eval"
predict_local_or_remote = "remote"
aisrv_connect_to_kaiwu_env_count = 1
need_to_sync = false

引用:
来源文件：project_default.toml 或平台评估配置片段
section：project_default
}
```

### 100. 评估框架不保存预测数据

```text
{
关键内容:
project_default 中 save_predict_data=false。

解读:
默认不会保存每一步 actor 输入输出数据。想复盘动作细节，需要依靠平台日志、视频、result JSON 或自己在可提交代码中加轻量日志；不能指望评估框架自动存下所有预测轨迹。

具体代码:
save_predict_data = false

引用:
来源文件：project_default.toml 或平台评估配置片段
section：project_default
}
```

### 101. Unitree RL Lab 是 Isaac Lab 扩展包来源

```text
{
关键内容:
extension.toml 声明 python.module name="unitree_rl_lab"，依赖 isaaclab、isaaclab_assets、isaaclab_mimic、isaaclab_rl、isaaclab_tasks。

解读:
地形、机器人资产、任务注册、velocity_env_cfg、events、terminations 等都来自 unitree_rl_lab 这个 Isaac Lab 扩展。继续挖比赛规则时，unitree_rl_lab/tasks、unitree_rl_lab/terrains、unitree_rl_lab/assets 是关键目录。

具体代码:
[[python.module]]
name = "unitree_rl_lab"

[dependencies]
"isaaclab" = {}
"isaaclab_assets" = {}
"isaaclab_rl" = {}
"isaaclab_tasks" = {}

引用:
来源文件：tools/unitree_rl_lab/config/extension.toml
section：package / dependencies / python.module
}
```

## 第五批：Unitree 执行器、机器人默认环境、课程学习、终止条件

### 102. 这批代码的性质：Isaac Lab 任务默认底座，不只是样例

```text
{
关键内容:
这批代码包含 Unitree 执行器模型、Go2 机器人资产配置、Isaac Lab 默认 PPO runner、课程学习函数、reset event、默认 MDP reward、完整 RobotEnvCfg、gym 注册入口。

解读:
它看起来像默认样例，是因为来自 unitree_rl_lab/Isaac Lab 扩展；但平台创建环境时会从这里的 RobotEnvCfg/RobotPlayEnvCfg 起步，再被 base_env.apply_usr_conf_to_env_cfg、RewardBridge、ObservationBridge 用你的 TOML 和 feature 代码覆盖。因此它是“默认底座 + 可被覆盖的源配置”，不是无关样例。

具体代码:
gym.register(
    id="Unitree-Go2-Velocity",
    entry_point="isaaclab.envs:ManagerBasedRLEnv",
    kwargs={
        "env_cfg_entry_point": "...RobotEnvCfg",
        "play_env_cfg_entry_point": "...RobotPlayEnvCfg",
        "rsl_rl_cfg_entry_point": "...BasePPORunnerCfg",
    },
)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/__init__.py
}
```

### 103. Go2 实际使用 UnitreeActuatorCfg_Go2HV

```text
{
关键内容:
UNITREE_GO2_CFG 中所有关节使用 UnitreeActuatorCfg_Go2HV，stiffness=25.0，damping=0.5，friction=0.01。Go2HV 扭矩速度参数为 X1=13.5、X2=30、Y1=20.2、Y2=23.4。

解读:
这是动作如何变成真实关节力矩的核心。最终 energy_score 读的是 applied_torque，而 applied_torque 会受这个执行器模型限制。高速度下最大力矩会下降，快速弹跳/甩腿不一定能给出想象中的力矩，还会拉高能耗。

具体代码:
UNITREE_GO2_CFG.actuators = {
    "GO2HV": UnitreeActuatorCfg_Go2HV(
        joint_names_expr=[".*"],
        stiffness=25.0,
        damping=0.5,
        friction=0.01,
    )
}

class UnitreeActuatorCfg_Go2HV:
    X1 = 13.5
    X2 = 30
    Y1 = 20.2
    Y2 = 23.4

引用:
来源文件：unitree_rl_lab/assets/robots/unitree.py
来源文件：unitree_rl_lab/assets/robots/unitree_actuators.py
}
```

### 104. 执行器力矩限制是速度相关的

```text
{
关键内容:
UnitreeActuator._clip_effort 根据 joint velocity 与 effort 是否同向选择 Y1/Y2，再在 |joint_vel| 超过 X1 后线性下降到 X2 处 0 力矩。

解读:
关节速度越高，可用力矩越小。高速跑跳会同时遇到两个问题：实际推力上限下降，以及 energy_score 中 |joint_vel| * |applied_torque| 变大。稳定快速步态比暴力高频动作更符合这个模型。

具体代码:
same_direction = (self._joint_vel * effort) > 0
max_effort = torch.where(same_direction, self._effort_y1, self._effort_y2)
max_effort = torch.where(
    self._joint_vel.abs() < self._velocity_x1,
    max_effort,
    self._compute_effort_limit(max_effort),
)

limit = k * (self._joint_vel.abs() - self._velocity_x1) + max_effort

引用:
来源文件：unitree_rl_lab/assets/robots/unitree_actuators.py
函数：UnitreeActuator._clip_effort / _compute_effort_limit
}
```

### 105. 执行器摩擦会直接改变 applied_effort

```text
{
关键内容:
UnitreeActuator.compute 在 DelayedPDActuator 计算后，从 applied_effort 中减去静摩擦项 Fs*tanh(joint_vel/Va) 和动摩擦项 Fd*joint_vel。

解读:
摩擦让小速度附近和持续运动时都要额外消耗力矩。Go2HV 配置里 Fs/Fd 默认 0，但其他执行器型号有非零摩擦。若平台实际只用 Go2HV，则主要关注 PD 和 torque-speed；若换机器人，摩擦会更明显。

具体代码:
self.applied_effort -= (
    self._friction_static * torch.tanh(joint_vel / self._activation_vel)
    + self._friction_dynamic * joint_vel
)

引用:
来源文件：unitree_rl_lab/assets/robots/unitree_actuators.py
函数：UnitreeActuator.compute
}
```

### 106. Go2 默认初始姿态和关节顺序

```text
{
关键内容:
Go2 默认 root 初始高度 pos=(0,0,0.4)，默认关节姿态：髋关节左右 ±0.1，大腿前腿 0.8、后腿 1.0，小腿 -1.5。joint_sdk_names 顺序为 FR、FL、RR、RL，每条腿 hip/thigh/calf。

解读:
动作输出 12 维对应这个关节顺序。use_default_offset=True 时 action 是围绕默认关节角的偏移。理解顺序很重要，否则做动作约束、镜像、步态分析时容易左右/前后搞反。

具体代码:
joint_sdk_names=[
    "FR_hip_joint", "FR_thigh_joint", "FR_calf_joint",
    "FL_hip_joint", "FL_thigh_joint", "FL_calf_joint",
    "RR_hip_joint", "RR_thigh_joint", "RR_calf_joint",
    "RL_hip_joint", "RL_thigh_joint", "RL_calf_joint",
]

init_state.joint_pos={
    ".*R_hip_joint": -0.1,
    ".*L_hip_joint": 0.1,
    "F[L,R]_thigh_joint": 0.8,
    "R[L,R]_thigh_joint": 1.0,
    ".*_calf_joint": -1.5,
}

引用:
来源文件：unitree_rl_lab/assets/robots/unitree.py
配置：UNITREE_GO2_CFG
}
```

### 107. 默认 action 是关节位置目标，scale=0.25

```text
{
关键内容:
ActionsCfg 使用 mdp.JointPositionActionCfg，所有关节 scale=0.25，use_default_offset=True，clip=(-100,100)。

解读:
actor 输出不是直接扭矩，而是关节目标位置偏移。默认意义上 action=1 约等于相对默认关节角偏移 0.25 rad。平台训练 workflow 还会先 clip action 到 [-6,6]，因此理论偏移范围可能很大，但执行器和关节限制会进一步约束。

具体代码:
JointPositionAction = mdp.JointPositionActionCfg(
    asset_name="robot",
    joint_names=[".*"],
    scale=0.25,
    use_default_offset=True,
    clip={".*": (-100.0, 100.0)},
)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：ActionsCfg
}
```

### 108. 默认 policy 观测包含 proprio + height_scan

```text
{
关键内容:
默认 PolicyCfg 顺序为 base_ang_vel(3)、projected_gravity(3)、velocity_commands(3)、joint_pos_rel(12)、joint_vel_rel(12)、last_action(12)、height_scan(256)，总计 301 维。

解读:
这是官方默认策略感知：身体角速度、姿态重力、速度命令、关节状态、上一动作、前方地形高度扫描。若你的 PolicyObservationProcess 调用 default_observation()，模型能看到这些；若覆盖后不调用，就会丢失。

具体代码:
base_ang_vel
projected_gravity
velocity_commands
joint_pos_rel
joint_vel_rel
last_action
height_scan

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：ObservationsCfg.PolicyCfg
}
```

### 109. 默认 critic 比 policy 多 base_lin_vel 和 joint_effort

```text
{
关键内容:
默认 CriticCfg 包含 base_lin_vel、base_ang_vel、projected_gravity、velocity_commands、joint_pos_rel、joint_vel_rel、joint_effort、last_action、height_scan，总计比 policy 多 base_lin_vel(3) 和 joint_effort(12)，约 316 维。

解读:
critic 有特权速度和力矩信息，可以更好估值；但正式评估 exploit 只给 actor obs，不给 critic_obs。训练时可用，推理时不能依赖。

具体代码:
base_lin_vel = ObsTerm(func=mdp.base_lin_vel)
joint_effort = ObsTerm(func=mdp.joint_effort, scale=0.01)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：ObservationsCfg.CriticCfg
}
```

### 110. height_scanner 的覆盖范围和分辨率

```text
{
关键内容:
height_scanner 是 16x16=256 rays，offset=(0.75,0,20)，ray_alignment="yaw"，GridPattern resolution=0.1，size=[1.5,1.5]，覆盖前方约 0~1.5m、横向 ±0.75m。

解读:
这是主要地形感知。它看得近但分辨率高，适合楼梯/坡/局部障碍。对于迷宫转向，默认还定义了 nav_scanner，但是否进入 policy 取决于 ObservationProcess 是否拼接。

具体代码:
height_scanner = RayCasterCfg(
    offset=RayCasterCfg.OffsetCfg(pos=(0.75, 0.0, 20.0)),
    ray_alignment="yaw",
    pattern_cfg=patterns.GridPatternCfg(resolution=0.1, size=[1.5, 1.5]),
)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：RobotSceneCfg
}
```

### 111. nav_scanner 是迷宫/导航前瞻感知，但默认 policy 未必使用

```text
{
关键内容:
nav_scanner 是 13x11=143 rays，offset=(1.25,0,20)，ray_alignment="base"，resolution=0.2，size=[2.5,2.0]，覆盖前方 2.5m、横向 ±1m。

解读:
它是为 L 型拐角、迷宫墙体前瞻设计的。默认 PolicyCfg 没列 nav_scanner，只有 height_scan；如果想让 actor 更会走迷宫，需要确认自定义 observation 是否拼入 nav_scanner 或等效目标/路径信息。

具体代码:
nav_scanner = RayCasterCfg(
    offset=RayCasterCfg.OffsetCfg(pos=(1.25, 0.0, 20.0)),
    ray_alignment="base",
    pattern_cfg=patterns.GridPatternCfg(resolution=0.2, size=[2.5, 2.0]),
)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：RobotSceneCfg
}
```

### 112. 默认地形块大小是 8m x 8m

```text
{
关键内容:
STANDARD_TERRAIN_CFG 和 TRACK_TERRAIN_CFG 的 size 都是 (8.0, 8.0)。

解读:
这直接影响 standard forward_score 和走穿阈值。默认半块长度 4m，走穿阈值 3.9m。Track 每段长度也是 8m，track_length=5 时整条赛道约 40m。

具体代码:
STANDARD_TERRAIN_CFG = StandardTerrainGeneratorCfg(size=(8.0, 8.0), ...)
TRACK_TERRAIN_CFG = TrackTerrainGeneratorCfg(size=(8.0, 8.0), ...)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
配置：STANDARD_TERRAIN_CFG / TRACK_TERRAIN_CFG
}
```

### 113. 默认 standard 地形组成

```text
{
关键内容:
Standard 默认地形包含 pyramid_slope、pyramid_slope_inv、pyramid_stairs、pyramid_stairs_inv、winding_corridor、maze，其中 winding_corridor 默认 proportion=0.0，maze 默认 proportion=0.1。

解读:
默认配置并不一定等于你的训练/评估配置，因为 base_env 会用 TOML 覆盖 proportion。但这些定义给出了各地形参数范围，例如坡度 0~0.4、楼梯高度 0.05~0.23、迷宫通道 0.6~1.0。

具体代码:
"pyramid_slope": slope_range=(0.0, 0.4)
"pyramid_stairs": step_height_range=(0.05, 0.23), step_width=0.3
"maze": wall_thickness=0.25, wall_height=0.5, passage_width_min=0.6

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
配置：STANDARD_TERRAIN_CFG
}
```

### 114. 默认 track 地形是线性赛道序列

```text
{
关键内容:
Track 默认 track_length=4，num_parallel_tracks=10，sub_terrains_order=["pyramid_slope", "pyramid_stairs", "pyramid_stairs_inv", "open_entry_maze"]，sub_terrains_random=False。

解读:
Track 是沿 X 方向串接多个 8m 地形段的线性课程。实际训练/评估可通过 TOML 覆盖 track_length 和 sub_terrains。若配置为 5 段，则 BaseScorer 的 max_segment_reached 范围会变为 0~4。

具体代码:
TRACK_TERRAIN_CFG = TrackTerrainGeneratorCfg(
    track_length=4,
    num_parallel_tracks=10,
    sub_terrains_random=False,
    sub_terrains_order=[...],
)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
配置：TRACK_TERRAIN_CFG
}
```

### 115. 评估模式启用地形边界终止

```text
{
关键内容:
_terrain_bounds_termination 在 env._enable_terrain_bounds_termination=True 时生效，机器人走出当前地形块 X/Y 半宽就终止。base_env 中该开关默认评估模式启用。

解读:
Standard 评估中横向乱跑或冲出块边界会触发终止。注释说它配合 time_out=True 使用，避免 termination penalty，但 scorer 仍会把未走穿记为失败。

具体代码:
out_of_bounds = (torch.abs(offset[:, 0]) > half_x) | (torch.abs(offset[:, 1]) > half_y)
return out_of_bounds

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
函数：_terrain_bounds_termination
}
```

### 116. goal_reached 终止阈值是 0.6m

```text
{
关键内容:
_goal_reached_termination 用 robot root XY 到 env.goal_positions XY 的距离判断，dist < threshold 即 done，默认 threshold=0.6。TerminationsCfg 注册 goal_reached threshold=0.6。

解读:
Track 完成系数来自这个 done term。必须进入目标 0.6m 范围内才算完成。reward reach_goal 的 threshold 也应保持 0.6，否则会出现“终止了但奖励没给到”的死区。

具体代码:
robot_pos = env.scene["robot"].data.root_pos_w[:, :2]
goal_pos = env.goal_positions[:, :2]
dist = torch.norm(goal_pos - robot_pos, dim=1)
reached = dist < threshold

goal_reached = DoneTerm(func=_goal_reached_termination, params={"threshold": 0.6})

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
函数：_goal_reached_termination
类：TerminationsCfg
}
```

### 117. 失败终止条件包括 base_contact 和 bad_orientation

```text
{
关键内容:
TerminationsCfg 中 base_contact 使用 base 接触力超过 threshold=1.0 判定非法接触；bad_orientation 使用 limit_angle=0.8。

解读:
趴地、翻倒、严重倾斜会提前结束 episode。Track 下这会导致 completion_coeff=0；Standard 下如果未走穿则 abnormal。跳跃策略如果落地姿态不稳，很容易触发 bad_orientation 或 base_contact。

具体代码:
base_contact = DoneTerm(
    func=mdp.illegal_contact,
    params={"sensor_cfg": SceneEntityCfg("contact_forces", body_names="base"), "threshold": 1.0},
)
bad_orientation = DoneTerm(func=mdp.bad_orientation, params={"limit_angle": 0.8})

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：TerminationsCfg
}
```

### 118. 默认 domain random 比用户 TOML 还能覆盖

```text
{
关键内容:
默认 EventCfg 启动时随机摩擦 [0.3,1.5]、restitution [0,0.15]、base mass add [-1,1] kg、link mass scale [0.9,1.1]、COM offset ±0.03m；interval push 默认每 4s，XY 速度 ±0.4。

解读:
这些是默认鲁棒性扰动。base_env.apply_usr_conf_to_env_cfg 会根据 [domain_rand] 覆盖摩擦和 push 等参数。正式评估是否启用，要看评估配置；训练时最好覆盖接近评估扰动。

具体代码:
static_friction_range=(0.3, 1.5)
mass_distribution_params=(-1.0, 1.0)
com_range={"x": (-0.03,0.03), ...}
push_robot.interval_range_s=(4.0, 4.0)
velocity_range={"x": (-0.4,0.4), "y": (-0.4,0.4)}

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：EventCfg
}
```

### 119. reset 逻辑：track 评估从 row=0 起点，standard maze 可随机出生

```text
{
关键内容:
reset_root_state_track_start 在 eval 或 curriculum_on 时 target_rows=0；standard/non-track 使用 reset_root_state_maze_random，maze 可从预计算 spawn_positions 随机格子出生。

解读:
Track 评估始终从赛道起点开始，训练 curriculum 也从 row=0 学完整赛道；非课程训练可能从非 maze 段随机起步做数据增强。Standard 迷宫出生点可能不是块中心。

具体代码:
if is_eval or curriculum_on:
    target_rows = torch.zeros(len(env_ids), dtype=torch.long, device=device)
else:
    non_maze_rows = _resolve_non_maze_rows(...)
    target_rows = non_maze_tensor[pick_idx]

reset_root_state_maze_random:
    if key in spawn_positions_map:
        positions[i, 0] = sp[0]
        positions[i, 1] = sp[1]

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/events.py
函数：reset_root_state_track_start / reset_root_state_maze_random
}
```

### 120. eval level list 会确定性分配 env 到难度/地形格

```text
{
关键内容:
reset_root_state_eval_level_aware 读取 env.cfg._eval_level_list。Standard: env_i -> row=level_list[(i//num_sub)%num_levels], col=i%num_sub。Track: row=0, col=level_list[i%num_levels]。

解读:
正式评估如果配置 level 列表，每个 env 会稳定落到指定难度和地形组合。复盘某个 env 的视频/分数时，可以用 env_id 推回 level 和 sub terrain。

具体代码:
Track:
row_indices = 0
col_indices = levels_tensor[env_idx_global % num_levels]

Standard:
sub_idx = env_idx_global % num_sub
level_idx = (env_idx_global // num_sub) % num_levels
row_indices = levels_tensor[level_idx]
col_indices = sub_idx

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/events.py
函数：reset_root_state_eval_level_aware
}
```

### 121. Track curriculum 升降的是 col，不是 row

```text
{
关键内容:
terrain_levels_vel 在 track 模式调用 _terrain_levels_vel_track。Track 成功条件是到达整条赛道 final_row 的出口，晋级/降级更新 terrain.terrain_types，也就是 col 难度档。

解读:
Track 的 row 是赛道段位置，不是难度；col 才是难度。训练中看到 terrain_levels 不升不代表 track 没 curriculum，要看 terrain_types/track_l{col}。

具体代码:
if _is_track_terrain(terrain_gen_cfg):
    return _terrain_levels_vel_track(...)

current_cols = terrain.terrain_types[env_ids].long()
move_up = dist_to_final_exit < exit_threshold
new_cols = current_cols + move_up.long() - move_down.long()
terrain.terrain_types[env_ids] = new_cols

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/curriculums.py
函数：terrain_levels_vel / _terrain_levels_vel_track
}
```

### 122. Standard 出口类地形用出口距离晋级

```text
{
关键内容:
Standard curriculum 默认用离 env_origin 的距离 > half_size 晋级；但 maze/corridor 这类出口地形会用 dist_to_exit < 0.5 覆盖晋级条件。

解读:
迷宫/走廊不是单纯走远就算学会，而是必须接近出口才升难度。训练时如果只刷距离但不找出口，maze/corridor 难度不会正常晋级。

具体代码:
move_up = distance > half_size

is_exit_terrain = exit_col_mask[types]
dist_to_exit = torch.norm(robot_xy - goal_xy, dim=1)
exit_move_up = dist_to_exit < 0.5
move_up = torch.where(is_exit_terrain, exit_move_up, move_up)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/curriculums.py
函数：terrain_levels_vel
}
```

### 123. command curriculum 根据 tracking reward 扩大速度采样范围

```text
{
关键内容:
lin_vel_cmd_levels / ang_vel_cmd_levels 会读取 reward_manager._episode_sums 中的 track_lin_vel_xy 或 track_ang_vel_z。如果平均 reward > reward_term.weight * 0.8，就把 command ranges 向 limit_ranges 扩大 0.1。

解读:
训练初期速度命令较小，跟踪做好后自动加大速度范围。若 track_lin_vel_xy reward 权重太低或模型跟踪不好，速度 curriculum 可能升不上去，最终 time_score 上限会受影响。

具体代码:
reward = mean(_episode_sums[reward_term_name][env_ids]) / env.max_episode_length_s
if reward > reward_term.weight * 0.8:
    ranges.lin_vel_x = clamp(ranges.lin_vel_x + [-0.1, 0.1], limit_ranges.lin_vel_x)
    ranges.lin_vel_y = clamp(...)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/curriculums.py
函数：lin_vel_cmd_levels / ang_vel_cmd_levels
}
```

### 124. 默认 command 初始范围与上限

```text
{
关键内容:
默认 base_velocity resampling_time=5s，初始 ranges: lin_vel_x/y=(-0.5,0.5)，ang_vel_z=(-1,1)；limit_ranges: lin_vel_x=(-2,2)，lin_vel_y=(-1.5,1.5)，ang_vel_z=(-1.5,1.5)。

解读:
这是默认速度跟踪任务的命令分布。你的训练 TOML 往往会覆盖成只允许前进的 lin_vel_x，例如 [0,0.8]。如果不覆盖，策略会学倒退/侧移/转向的通用速度跟踪，不一定最适合比赛评分。

具体代码:
resampling_time_range=(5.0, 5.0)
ranges=Ranges(lin_vel_x=(-0.5,0.5), lin_vel_y=(-0.5,0.5), ang_vel_z=(-1.0,1.0))
limit_ranges=Ranges(lin_vel_x=(-2.0,2.0), lin_vel_y=(-1.5,1.5), ang_vel_z=(-1.5,1.5))

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：CommandsCfg
}
```

### 125. 默认 MDP reward 与平台 score 不同，但能反向塑造 score

```text
{
关键内容:
默认 RewardsCfg 包含速度跟踪、z 速度、角速度、关节速度/加速度/力矩、action_rate、关节限位、energy、flat_orientation、joint_pos、feet_air_time、air_time_variance、feet_slide、undesired_contacts 等。

解读:
这些是默认训练奖励，不是最终平台得分。但它们与 score 有间接关系：energy/torque/action_rate 可压能耗，flat_orientation 可压姿态偏移，速度/feet reward 可提高完成速度和稳定性。

具体代码:
track_lin_vel_xy weight=1.5
track_ang_vel_z weight=0.75
energy weight=-2e-5
flat_orientation_l2 weight=-2.5
joint_torques weight=-2e-4
action_rate weight=-0.1

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/velocity_env_cfg.py
类：RewardsCfg
}
```

### 126. mdp.energy 与平台 energy_score 的单步能耗量一致

```text
{
关键内容:
mdp.rewards.energy 返回 sum(abs(joint_vel) * abs(applied_torque))，与 BaseScorer 中 energy_per_env 的定义一致。

解读:
训练 reward 里的 energy 惩罚可以直接对齐平台 energy_score 的原始量。区别是训练 reward 是每步加权惩罚，平台 score 是 episode 平均后做 exp(-0.01*x)。

具体代码:
qvel = asset.data.joint_vel[:, asset_cfg.joint_ids]
qfrc = asset.data.applied_torque[:, asset_cfg.joint_ids]
return torch.sum(torch.abs(qvel) * torch.abs(qfrc), dim=-1)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/rewards.py
函数：energy
}
```

### 127. gait_phase 是可选周期步态感知

```text
{
关键内容:
gait_phase(period) 根据 episode_length_buf * step_dt 计算全局相位，并输出 sin/cos 两维。

解读:
这可以作为 policy 输入或 reward 辅助，让策略形成固定周期步态。它本身不参与最终 score，但稳定周期步态可能降低姿态和能耗波动。

具体代码:
global_phase = (env.episode_length_buf * env.step_dt) % period / period
phase[:, 0] = torch.sin(global_phase * torch.pi * 2.0)
phase[:, 1] = torch.cos(global_phase * torch.pi * 2.0)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/mdp/observations.py
函数：gait_phase
}
```

### 128. 默认 PPO runner 是 Isaac Lab/RSL-RL 基线，不一定是你提交 PPO 的实际超参

```text
{
关键内容:
BasePPORunnerCfg 设置 num_steps_per_env=24、max_iterations=50000、save_interval=100、actor/critic hidden=[512,256,128]、ELU、PPO clip=0.2、entropy=0.01、lr=1e-3、gamma=0.99、lam=0.95。

解读:
这是 unitree_rl_lab 注册给 rsl_rl 的默认 runner 配置。当前 Kaiwu 项目里的 agent_ppo 有自己的一套 AlgorithmPPO/ActorCritic/Config，未必直接使用这个 runner；但它能反映官方基线规模和常用 PPO 参数。

具体代码:
policy = RslRlPpoActorCriticCfg(
    actor_hidden_dims=[512, 256, 128],
    critic_hidden_dims=[512, 256, 128],
    activation="elu",
)
algorithm = RslRlPpoAlgorithmCfg(
    clip_param=0.2,
    entropy_coef=0.01,
    learning_rate=1.0e-3,
    gamma=0.99,
    lam=0.95,
)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/agents/rsl_rl_ppo_cfg.py
类：BasePPORunnerCfg
}
```

## 第六批：迷宫/赛道地形生成、出口目标与导航难点

### 129. Maze 地形的难度核心是通道宽度

```text
{
关键内容:
maze / eroded_maze / open_entry_eroded_maze 的 curriculum 都用 difficulty 控制 passage_width：passage_width = passage_width_max - (passage_width_max - passage_width_min) * difficulty。默认 passage_width_max=1.0m，passage_width_min=0.6m。

解读:
难度越高，通道越窄。Go2 需要在 0.6m 通道里转向、避墙、到出口。策略在低难度能过，不代表高难度能过；高难度主要考验横向控制、转向半径、姿态稳定和墙体感知。

具体代码:
passage_width = cfg.passage_width_max - (cfg.passage_width_max - cfg.passage_width_min) * difficulty

passage_width_max: float = 1.0
passage_width_min: float = 0.6

引用:
来源文件：unitree_rl_lab/terrains/maze_terrain.py
来源文件：unitree_rl_lab/terrains/eroded_maze_terrain.py
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
}
```

### 130. 普通 maze 使用 DFS 生成，并保证入口到出口连通

```text
{
关键内容:
Maze 使用 randomized DFS/Recursive Backtracker 生成墙体，并用 BFS 验证 start cell 到 goal cell 连通。入口在左边界，出口在右边界。

解读:
迷宫不是随机死路堆叠，理论上一定存在路径。失败通常不是“无路可走”，而是策略感知/转向/避墙不够。训练时应针对导航和墙体避障，而不是只做速度跟踪。

具体代码:
h_walls, v_walls = _generate_maze_dfs(...)
if _verify_connectivity_grid(..., (entry_cx, entry_cy), (exit_cx, exit_cy)):
    break

entry_cx, entry_cy = 0, 0
exit_cx, exit_cy = n_cells_x - 1, n_cells_y - 1

引用:
来源文件：unitree_rl_lab/terrains/maze_terrain.py
函数：maze_terrain / _generate_maze_dfs / _verify_connectivity_grid
}
```

### 131. maze 出口位置会写入 exit_info，供 goal_positions 使用

```text
{
关键内容:
Maze terrain 生成时会计算 exit_info，包括 position、yaw 和 spawn_positions。出口在右边界附近，yaw=0，表示朝 +X。

解读:
Track/standard 的 goal_positions 最终会从这些 exit_info 或默认出口计算得到。Track 的 goal_reached 判定目标不是任意点，而是这些地形生成器记录的出口位置。

具体代码:
exit_info = {
    "position": (exit_world_x, exit_world_y, 0.0),
    "yaw": 0.0,
    "spawn_positions": spawn_positions,
}

引用:
来源文件：unitree_rl_lab/terrains/maze_terrain.py
来源文件：unitree_rl_lab/terrains/eroded_maze_terrain.py
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
}
```

### 132. spawn_positions 是迷宫随机出生和复现的重要数据

```text
{
关键内容:
Maze 生成器会把所有 cell center 作为 spawn_positions 放进 exit_info。Standard/Track generator 会把这些 local spawn positions 转成 world 坐标并保存到 terrain_spawn_positions。

解读:
standard 的 maze reset 可以从这些 cell center 随机出生。也就是说迷宫不一定从固定入口开始，尤其 standard 模式下要具备从不同格子出发的能力。

具体代码:
spawn_positions.append((sx, sy, 0.0))
exit_info["spawn_positions"] = spawn_positions

self.terrain_spawn_positions[(sub_row, sub_col)] = world_spawns

引用:
来源文件：unitree_rl_lab/terrains/maze_terrain.py
来源文件：unitree_rl_lab/terrains/track_generator.py
函数：maze_terrain / StandardTerrainGenerator._store_exit_info / TrackTerrainGenerator._store_exit_info
}
```

### 133. ErodedMaze 会削掉拐角，降低卡墙概率

```text
{
关键内容:
ErodedMaze 在 L/T/十字墙角处移除 corner post，并删除连接墙段靠近拐角的一半；boundary corner 还可以更大比例削墙。

解读:
这不是普通 90 度硬拐角迷宫，而是对腿式机器人更友好的圆滑/开口拐角。策略仍需转向，但不必贴着尖角硬转。训练时 wall_proximity 和路径引导会比“精确贴墙导航”更有效。

具体代码:
post_eroded[iy, ix] = True
h_erode_left / h_erode_right / v_erode_top / v_erode_bottom
boundary_erosion_fraction: float = 0.80

引用:
来源文件：unitree_rl_lab/terrains/eroded_maze_terrain.py
函数：_compute_corner_erosion / eroded_maze_terrain
}
```

### 134. OpenEntryErodedMaze 左边界完全打开，用于 Track 段间衔接

```text
{
关键内容:
OpenEntryErodedMaze 与 ErodedMaze 类似，但整个左边界 X=0 被移除，左边界没有墙段和 corner posts。

解读:
这是 track 里接在上一段后面的迷宫版本。机器人可以从任意 Y 位置进入迷宫，不会因为段间对齐误差撞上左边界。Track 训练应优先适应 open_entry_maze，而不是只练普通 maze。

具体代码:
# LEFT BOUNDARY (X=0): ENTIRELY REMOVED
# No left boundary wall segments are drawn at all.

if ix == 0:
    continue

引用:
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
函数：open_entry_eroded_maze_terrain
}
```

### 135. OpenEntryErodedMaze 的出口在右边界中部，而不是右上角

```text
{
关键内容:
open_entry_eroded_maze 把 exit_cy 设置为 n_cells_y // 2，出口位于右边界中部附近；普通 NavMaze/ErodedMaze 通常使用右上/最后一行 cell。

解读:
Track 中 open_entry_maze 的最终目标可能在迷宫右侧中部，不是固定右上角。策略如果学了“总往右上角钻”，可能在 open_entry_maze 里偏离出口。

具体代码:
exit_cx, exit_cy = n_cells_x - 1, n_cells_y // 2
exit_world_x = terrain_x - wall_thickness / 2.0
exit_world_y = exit_center_y

引用:
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
函数：open_entry_eroded_maze_terrain
}
```

### 136. OpenEntryErodedMaze 有出口屏风墙，禁止直线冲终点

```text
{
关键内容:
OpenEntryErodedMaze 默认 exit_screen_enabled=True，会在出口前方放一段 screen wall。屏风墙位于出口向内约 1 个 cell，Y 长度是出口开口宽度的 2.5 倍，强制机器人从两侧绕过去。

解读:
这是 Track maze 最关键的导航陷阱：不能只朝 goal_positions 直线冲。策略需要识别出口前屏风并绕行。仅用“朝目标速度”reward 可能会撞屏风，必须有墙体/路径感知或避障 reward。

具体代码:
exit_screen_enabled: bool = True
exit_screen_offset_cells: float = 1.0
exit_screen_width_factor: float = 2.5

screen_x = terrain_x - wall_thickness - screen_offset_x
screen_y = exit_center_y
_add_box(screen_x, screen_y, half_h, wall_thickness, screen_length_y, wall_height)

引用:
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
类：OpenEntryErodedMazeTerrainCfg
函数：open_entry_eroded_maze_terrain
}
```

### 137. OpenEntryErodedMaze 会清理屏风墙 AABB 附近的内部墙

```text
{
关键内容:
在生成屏风墙前，代码会计算 screen wall AABB，并移除与其重叠的内部 h_walls、v_walls 和 posts。

解读:
屏风墙不是叠在随机迷宫墙上的混乱结构，而是会先清出屏风附近空间。导航上通常是“出口前有一个明确障碍，需要绕左或绕右”，不是完全不可通行。

具体代码:
if _aabb_overlaps(..., _scr_x_lo, _scr_y_lo, _scr_x_hi, _scr_y_hi):
    h_walls[cy, cx] = False

if _aabb_overlaps(...):
    v_walls[cy, cx] = False

if _aabb_overlaps(...):
    post_eroded[iy, ix] = True

引用:
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
函数：open_entry_eroded_maze_terrain
}
```

### 138. TerrainExitManager 是 goal_positions 的核心来源

```text
{
关键内容:
TerrainExitManager 存储 terrain_exit_positions 和 terrain_exit_yaws，支持 standard grid 和 track linear 两种查询。observation_process.update_goal_positions 会调用它刷新 env.goal_positions。

解读:
平台导航目标不是模型内部算出来的，而是地形生成阶段预计算/记录的出口。Track 完成、goal reward、goal observation 都依赖这个管理器。

具体代码:
self._exit_positions: torch.Tensor | None = None
self._exit_yaws: torch.Tensor | None = None

goal_positions = self._exit_positions[levels_clamped, types_clamped]
goal_positions = self._exit_positions[finish_row, types_clamped]

引用:
来源文件：unitree_rl_lab/terrains/terrain_exit_manager.py
类：TerrainExitManager
}
```

### 139. 默认出口是 X 正向边缘中心附近，距边缘 0.5m

```text
{
关键内容:
非自定义地形若没有 exit_info，会使用默认出口：exit_x = block_origin_x + size_x - 0.5，exit_y = block_origin_y + size_y/2，yaw=0。

解读:
坡、楼梯等非迷宫地形的 goal 通常在块的 +X 方向边缘中心附近。Standard/Track 的默认“通过”方向就是 +X。

具体代码:
exit_x = block_origin_world[0] + terrain_size[0] - 0.5
exit_y = block_origin_world[1] + terrain_size[1] / 2.0
exit_yaw = 0.0

引用:
来源文件：unitree_rl_lab/terrains/terrain_exit_manager.py
函数：compute_default_exit_for_block
来源文件：unitree_rl_lab/terrains/track_generator.py
函数：compute_default_exit_info
}
```

### 140. Track 的 goal 永远是整条赛道最后一段出口

```text
{
关键内容:
TerrainExitManager.get_track_goal_positions 固定使用 finish_row = track_length - 1，不再根据 robot_pos_x 逐段递进。

解读:
Track 中目标点始终是最终出口，不是当前段出口。策略看到 goal 时会看到一个远目标，穿越 slope/stairs/maze 都要朝最终出口推进。局部避障仍要靠 height/nav scanner 或 reward。

具体代码:
finish_row = self._track_length - 1
types_clamped = terrain_types.clamp(0, self._num_cols - 1).long()
goal_positions = self._exit_positions[finish_row, types_clamped]

引用:
来源文件：unitree_rl_lab/terrains/terrain_exit_manager.py
函数：TerrainExitManager.get_track_goal_positions
}
```

### 141. TrackTerrainGenerator 的 row 是 X 方向段，col 是 Y 方向平行赛道/难度

```text
{
关键内容:
TrackTerrainGenerator 将 track_length 映射到 num_rows，num_parallel_tracks 映射到 num_cols。Isaac Lab 坐标约定 row→X，col→Y。

解读:
这解释了为什么 Track 的 difficulty 按 col 分组，max_segment_reached 按 root_pos_x 算。不要用 standard 的 row=难度理解 track。

具体代码:
cfg.num_rows = cfg.track_length
cfg.num_cols = cfg.num_parallel_tracks

terrain_origin_x = sub_row * sub_cfg.size[0]
terrain_origin_y = sub_col * sub_cfg.size[1]

引用:
来源文件：unitree_rl_lab/terrains/track_generator.py
类：TrackTerrainGenerator
函数：__init__ / _add_sub_terrain
}
```

### 142. Track 难度是确定性 col→difficulty，无随机抖动

```text
{
关键内容:
TrackTerrainGenerator 在 curriculum 和 random 两条生成路径中都使用 difficulty = lower + (upper-lower) * (col / num_cols)，无随机 jitter。

解读:
Track 的 col=k 就是固定难度档。评估 track_l0、track_l1 等分数可以稳定反映难度递增表现。训练时 curriculum=true 和 false 的区别主要是 reset 初始化 col 的分布，不是 col 到难度的映射。

具体代码:
difficulty = sub_col / self.cfg.num_cols
difficulty = lower + (upper - lower) * difficulty

引用:
来源文件：unitree_rl_lab/terrains/track_generator.py
函数：TrackTerrainGenerator._generate_curriculum_terrains / _generate_random_terrains
}
```

### 143. Track 的固定序列由 sub_terrains_order 决定，可循环/截断

```text
{
关键内容:
sub_terrains_random=False 时，Track 使用 sub_terrains_order 生成每条赛道序列；如果序列长度小于 track_length，会循环扩展，再截断到 track_length。

解读:
正式赛道每一段的类型顺序不是随机的，除非配置 sub_terrains_random=True。你可以从 eval 配置的 sub_terrains_order/sub_terrains 推出第 0、1、2、3、4 段分别是什么，从而针对性训练。

具体代码:
for name in cfg.sub_terrains_order:
    sequence.append(cfg.sub_terrains[name])
while len(sequence) < cfg.track_length:
    sequence.extend(sequence)
return sequence[: cfg.track_length]

引用:
来源文件：unitree_rl_lab/terrains/track_generator.py
函数：TrackTerrainGenerator._get_track_sequence
}
```

### 144. Track 有侧墙隔离平行赛道

```text
{
关键内容:
TrackTerrainGenerator 默认 track_wall_enabled=True，会在每个 col 边界放置侧墙，墙沿 X 跨完整赛道，默认高度 1.0m、厚度 0.15m。

解读:
Track 不能通过横向跑到相邻低难度赛道取巧。侧墙也会让横向控制失误更容易撞墙或卡住。策略应尽量保持在当前赛道中心附近。

具体代码:
track_wall_enabled: bool = True
track_wall_height: float = 1.0
track_wall_thickness: float = 0.15

for col in range(cfg.num_cols + 1):
    wall_y = col * cfg.size[1]
    wall = trimesh.creation.box(wall_dims, ...)

引用:
来源文件：unitree_rl_lab/terrains/track_generator.py
类：TrackTerrainGeneratorCfg
函数：TrackTerrainGenerator._add_terrain_border
}
```

### 145. Track/Standard generator 都会把 exit/spawn 数据回写到 cfg

```text
{
关键内容:
TrackTerrainGenerator 和 StandardTerrainGenerator 初始化结束后，会把 terrain_exit_positions、terrain_exit_yaws、terrain_spawn_positions 写回 cfg。

解读:
Isaac Lab TerrainImporter 可能不保留 generator 实例，所以平台通过 cfg 读取运行期出口/出生点数据。goal_positions 和迷宫随机出生依赖这些回写字段。

具体代码:
cfg.terrain_exit_positions = self.terrain_exit_positions
cfg.terrain_exit_yaws = self.terrain_exit_yaws
cfg.terrain_spawn_positions = self.terrain_spawn_positions

引用:
来源文件：unitree_rl_lab/terrains/track_generator.py
类：TrackTerrainGenerator / StandardTerrainGenerator
}
```

### 146. Standard curriculum 的地形类型按 proportion 分配到列

```text
{
关键内容:
StandardTerrainGenerator curriculum 模式先按 sub_terrain proportion 计算 col→sub_index；每一列固定一种子地形，row 决定难度。

解读:
Standard 的地形类型不是每个格子完全随机，而是列上固定分段。BaseScorer 用同样的 proportion 逻辑反推 terrain_names，因此监控的 terrain_type 分组可信。

具体代码:
proportions = np.array([sub_cfg.proportion for sub_cfg in self.cfg.sub_terrains.values()])
sub_index = np.min(np.where(index / self.cfg.num_cols + 0.001 < np.cumsum(proportions))[0])

for sub_col in range(self.cfg.num_cols):
    for sub_row in range(self.cfg.num_rows):
        difficulty = (sub_row + self.np_rng.uniform()) / self.cfg.num_rows

引用:
来源文件：unitree_rl_lab/terrains/track_generator.py
函数：StandardTerrainGenerator._generate_curriculum_terrains
}
```

### 147. WindingCorridor 是保证可通过的障碍走廊

```text
{
关键内容:
WindingCorridor 生成 X 方向走廊，两侧高墙，内部随机放障碍。每个障碍与墙、障碍之间至少保留 robot_passable_width，默认 0.6m。

解读:
这个地形考验绕障和横向控制，但理论上总有可通过路径。Go2 在 0.6m clearance 下需要精细转向，过度大步幅或身体横摆容易撞障碍/墙。

具体代码:
robot_passable_width: float = 0.6

if wc < passable:
    continue
if gap < passable:
    too_close = True

引用:
来源文件：unitree_rl_lab/terrains/winding_corridor_terrain.py
类：WindingCorridorTerrainCfg
函数：winding_corridor_terrain
}
```

### 148. WindingCorridor 难度同时改变宽度、障碍数量和尺寸

```text
{
关键内容:
WindingCorridor 难度越高，走廊越窄，障碍数量越多，障碍宽度和深度上限越大。默认 corridor_width 从 4.0m 变到 2.0m，障碍数从 3 到 12。

解读:
高难度 corridor 不只是墙更近，还会更拥挤。Track 或 Standard 如果包含 winding_corridor，需要训练避障而不是只直线冲。

具体代码:
corridor_width = max_width + difficulty * (min_width - max_width)
num_obstacles = min + difficulty * (max - min)
obs_w_max = obs_w_min + difficulty * (obs_w_range)
obs_d_max = obs_d_min + difficulty * (obs_d_range)

引用:
来源文件：unitree_rl_lab/terrains/winding_corridor_terrain.py
函数：winding_corridor_terrain
}
```

### 149. WindingCorridor 出口是 +X 方向末端中心

```text
{
关键内容:
WindingCorridor origin 在起点清空区中心，exit_info 的 position 是 x=terrain_x-exit_clearance/2, y=corridor center, yaw=0。

解读:
这个地形的目标方向明确沿 +X。策略应沿走廊中心推进并绕过障碍，最终回到中心线附近。

具体代码:
origin_x = spawn_clearance / 2.0
origin_y = corridor_y_center

exit_x = terrain_x - exit_clearance / 2.0
exit_y = corridor_y_center
exit_info = {"position": (exit_x, exit_y, 0.0), "yaw": 0.0}

引用:
来源文件：unitree_rl_lab/terrains/winding_corridor_terrain.py
函数：winding_corridor_terrain
}
```

### 150. 对 Track open_entry_maze 的训练启示

```text
{
关键内容:
Track 的 open_entry_maze 左侧全开，出口在右侧中部，出口前有屏风墙，通道宽度随难度从 1.0m 缩到 0.6m，角落被 erosion 放宽。

解读:
这说明 Track 末段不能靠“向终点直线速度”单独解决。更有效的策略是：使用前方/导航扫描识别屏风和墙，保留足够横向速度和 yaw 控制能力，靠近出口时允许绕行，而不是强惩罚所有侧移/转向。

具体代码:
OpenEntryErodedMazeTerrainCfg:
    passage_width_max=1.0
    passage_width_min=0.6
    boundary_erosion_fraction=0.80
    exit_screen_enabled=True
    exit_screen_width_factor=2.5

引用:
来源文件：unitree_rl_lab/terrains/open_entry_eroded_maze_terrain.py
来源文件：unitree_rl_lab/terrains/track_generator.py
}
```

## 第七批：部署导出、配置校验、监控指标与旧版统计工具

### 151. export_deploy_cfg 会导出真实部署所需的控制参数

```text
{
关键内容:
export_deploy_cfg(env, log_dir) 会从 Isaac 环境中读取机器人关节、控制周期、PD 参数、动作配置、观测配置，并写成 log_dir/params/deploy.yaml。

解读:
这不是训练 reward，也不是评分逻辑，而是把训练环境里的关键控制参数固化给部署/推理端用。真正上机器人或做离线推理时，需要保证 deploy.yaml 里的 step_dt、joint_ids_map、action scale、offset、clip 和训练时一致。

具体代码:
cfg["joint_ids_map"] = joint_ids_map
cfg["step_dt"] = env.cfg.sim.dt * env.cfg.decimation
cfg["stiffness"] = stiffness.tolist()
cfg["damping"] = damping.tolist()
filename = os.path.join(log_dir, "params", "deploy.yaml")

引用:
来源文件：unitree_rl_lab/utils/export_deploy_cfg.py
函数：export_deploy_cfg
}
```

### 152. deploy.yaml 使用 joint_sdk_names 建立仿真关节到 SDK 关节的映射

```text
{
关键内容:
导出逻辑读取 env.cfg.scene.robot.joint_sdk_names，并用 resolve_matching_names(asset.data.joint_names, joint_sdk_names, preserve_order=True) 生成 joint_ids_map。

解读:
Unitree SDK 的关节顺序和 Isaac 内部 articulation joint 顺序可能不同。joint_ids_map 是部署端重排关节数据的关键，顺序错了会导致某个 action 控错关节，是非常严重的问题。

具体代码:
joint_sdk_names = env.cfg.scene.robot.joint_sdk_names
joint_ids_map, _ = resolve_matching_names(asset.data.joint_names, joint_sdk_names, preserve_order=True)

引用:
来源文件：unitree_rl_lab/utils/export_deploy_cfg.py
依赖：env.cfg.scene.robot.joint_sdk_names
}
```

### 153. deploy.yaml 会导出 JointPositionAction 的 scale、clip 和 offset

```text
{
关键内容:
动作项导出时会把 action scale 展开到每个动作维度；如果配置了 clip，也会导出真实 clip；JointPositionAction / JointVelocityAction 会导出 offset。

解读:
训练里的 action 不是直接关节角。以默认 JointPositionAction 为例，策略输出会乘 scale，再加 default joint offset。部署端如果漏掉 offset 或 scale，动作幅度和姿态基准都会错。

具体代码:
if isinstance(term_cfg.scale, float):
    term_cfg.scale = [term_cfg.scale for _ in range(action_term.action_dim)]
else:
    term_cfg.scale = action_term._scale[0].detach().cpu().numpy().tolist()

if term_cfg.use_default_offset:
    term_cfg.offset = action_term._offset[0].detach().cpu().numpy().tolist()

引用:
来源文件：unitree_rl_lab/utils/export_deploy_cfg.py
函数：export_deploy_cfg
}
```

### 154. deploy.yaml 会导出 policy observation 的 scale、clip 和 history_length

```text
{
关键内容:
导出逻辑遍历 observation_manager 的 policy 组，实际调用每个 obs func 计算维度，然后记录 scale、clip、history_length。

解读:
部署端必须复现训练时的观测归一化/缩放，否则同一个策略网络输入分布会变。尤其是 height_scan、joint_vel_rel、base_ang_vel 这类有 scale 的项，缺一个 scale 都会明显掉性能。

具体代码:
obs_names = env.observation_manager.active_terms["policy"]
obs_dims = tuple(obs_cfg.func(env, **obs_cfg.params).shape)
if term_cfg.scale is not None:
    scale = term_cfg.scale.detach().cpu().numpy().tolist()
if term_cfg.history_length == 0:
    term_cfg.history_length = 1

引用:
来源文件：unitree_rl_lab/utils/export_deploy_cfg.py
函数：export_deploy_cfg
}
```

### 155. deploy.yaml 的 commands 默认导出 limit_ranges 而不是当前 ranges

```text
{
关键内容:
如果 base_velocity 命令配置存在 limit_ranges，部署配置优先导出 limit_ranges；否则才导出 ranges。

解读:
这表示部署端看到的是命令允许范围的上限，而不一定是训练初期 curriculum 的当前采样范围。比如训练初始 lin_vel_x 可能是 [-0.5, 0.5]，但 limit 是 [-2.0, 2.0]。

具体代码:
if hasattr(env.cfg.commands.base_velocity, "limit_ranges"):
    ranges = env.cfg.commands.base_velocity.limit_ranges.to_dict()
else:
    ranges = env.cfg.commands.base_velocity.ranges.to_dict()

引用:
来源文件：unitree_rl_lab/utils/export_deploy_cfg.py
函数：export_deploy_cfg
}
```

### 156. parse_env_cfg 是 Isaac Lab 任务配置加载入口包装

```text
{
关键内容:
parse_env_cfg(task_name, device, num_envs, use_fabric) 从 gym registry 加载 env_cfg_entry_point，然后覆盖 sim.device、sim.use_fabric、scene.num_envs。

解读:
这主要用于脚本启动环境时改 GPU、环境数量或 Fabric 读写模式。它不改 reward、terrain 或 policy 结构，只是加载并覆盖少数运行参数。

具体代码:
cfg = load_cfg_from_registry(task_name, entry_point_key)
cfg.sim.device = device
if use_fabric is not None:
    cfg.sim.use_fabric = use_fabric
if num_envs is not None:
    cfg.scene.num_envs = num_envs

引用:
来源文件：unitree_rl_lab/utils/parse_cfg.py
函数：parse_env_cfg
}
```

### 157. ExampleExtension 是 Omniverse 扩展模板，基本不影响比赛逻辑

```text
{
关键内容:
ExampleExtension 只是在 Isaac/Omniverse 启动时创建一个测试窗口，按钮 Add/Reset 改 label。some_public_function 返回 x**x。

解读:
这明显是扩展模板/占位代码，不是训练、评估、地形、奖励或控制逻辑。参赛调参时可以忽略。

具体代码:
class ExampleExtension(omni.ext.IExt):
    def on_startup(self, ext_id):
        self._window = omni.ui.Window("My Window", width=300, height=300)

def some_public_function(x: int):
    return x**x

引用:
来源文件：unitree_rl_lab/extension.py
}
```

### 158. setup.py 说明该 Isaac 扩展包的安装元信息

```text
{
关键内容:
setup.py 从 config/extension.toml 读取包信息，包名是 unitree_rl_lab，Python 要求 >=3.10，分类标注 Isaac Sim 4.5.0，额外依赖只有 psutil。

解读:
这说明这个目录是 Isaac Lab extension/python package。它不是算法主体，也不定义训练超参；主要作用是让 Isaac 能发现并加载 unitree_rl_lab。

具体代码:
setup(
    name="unitree_rl_lab",
    packages=["unitree_rl_lab"],
    install_requires=["psutil"],
    python_requires=">=3.10",
)

引用:
来源文件：setup.py
}
```

### 159. get_training_metrics 决定训练监控面板上报哪些指标

```text
{
关键内容:
get_training_metrics 动态读取 monitor_default.yaml 中 env_global 和 terrain_ 组的 env 指标，再加 basic、algorithm、reward 三组固定指标，最后交给 collect_training_metrics。

解读:
这影响 dashboard/监控显示，不一定等同最终考试评分。env 指标中 count 类用 sum，其余用 avg；algorithm/reward 大多用 avg。

具体代码:
env_keys = load_env_keys_from_monitor_config()
env_metrics[key] = "sum" if is_count else "avg"

metrics_dict = {
    "basic": {...},
    "algorithm": {...},
    "reward": {...},
    "env": env_metrics,
}

引用:
来源文件：tools/monitor.py 或项目监控入口
函数：get_training_metrics
}
```

### 160. monitor env 指标来自 /root/tools/conf/monitor_default.yaml

```text
{
关键内容:
load_env_keys_from_monitor_config 会扫描 monitor_default.yaml 里 group_name_en 为 env_global 或以 terrain_ 开头的组，从 Prometheus 表达式 kaiwu_xxx{} 中提取 xxx 作为指标 key。

解读:
这解释了为什么监控面板会自动出现 completed_task_count、terrain level 分数等指标。指标名不一定硬编码在 Python 里，而是从 monitor yaml 动态提取。

具体代码:
if group_name_en == "env_global" or group_name_en.startswith("terrain_"):
    match = re.search(r"kaiwu_(\\w+)\\{", expr)
    key = match.group(1)

引用:
来源文件：tools/utils.py
函数：load_env_keys_from_monitor_config
}
```

### 161. reward 监控 key 支持 standard 和 track 两种分组

```text
{
关键内容:
load_reward_keys_from_monitor_config 既支持 group_name_en == "reward"，也支持 group_name_en 以 reward_ 开头的分组，比如 reward_locomotion / reward_navigation。

解读:
这说明 Track/Nav 任务可能把 reward 监控拆成多组。函数会跳过 episode_reward、reward_mean、reward_std，只保留具体单项 reward。

具体代码:
if group_en == "reward" or group_en.startswith("reward_"):
    if metrics_name and metrics_name not in _SKIP_METRICS:
        match = re.search(r"kaiwu_(\\w+)\\{", expr)

引用:
来源文件：tools/utils.py
函数：load_reward_keys_from_monitor_config
}
```

### 162. new-generation TOML 直接读取，不做 base/global 合并

```text
{
关键内容:
read_usr_conf 直接 toml.load 用户配置文件，注释明确写着 No base/global merging，TOML 文件自包含所有配置。

解读:
这和一些旧版配置系统不同。对于 new-generation standard/track 配置，用户 TOML 缺必填项就会报错，不要假设一定会从 base toml 自动补齐所有字段。

具体代码:
with open(usr_conf_file, "r", encoding="utf-8") as f:
    config = toml.load(f)
logger.info(f"[new-gen] Loaded config directly: {usr_conf_file}")

引用:
来源文件：tools/config_checker.py
函数：read_usr_conf
}
```

### 163. 配置校验会一次性收集所有错误

```text
{
关键内容:
check_usr_conf 会依次校验 env、init_state、terrain、domain_rand、noise、commands，训练模式还会校验 rewards；所有错误一起返回。

解读:
这对调 TOML 很有用。不是遇到第一个错就停，而是一次告诉你所有不合法字段，适合提交前做配置体检。

具体代码:
all_errors.extend(_validate_env_params(usr_conf, is_eval))
all_errors.extend(_validate_init_state_params(usr_conf))
all_errors.extend(_validate_terrain_params(usr_conf, is_eval))
...
if not is_eval:
    all_errors.extend(_validate_rewards_params(usr_conf))

引用:
来源文件：tools/config_checker.py
函数：check_usr_conf
}
```

### 164. [env] 配置的硬限制：episode_length_s 必填，训练 num_envs 必填且 1-4096

```text
{
关键内容:
[env] episode_length_s 必须存在且 >0；num_envs 在训练配置中必填，评估配置可选，取值必须是整数且 1 到 4096。

解读:
如果训练 TOML 不写 num_envs 会直接校验失败。4096 是平台允许的最大环境数量，不是一定推荐拉满；显存和吞吐仍要看机器。

具体代码:
if "episode_length_s" not in env_conf:
    errors.append("[env] Missing required parameter: episode_length_s")
elif episode_length_s <= 0:
    errors.append(...)

elif not (1 <= num_envs <= 4096):
    errors.append(...)

引用:
来源文件：tools/config_checker.py
函数：_validate_env_params
}
```

### 165. [init_state] 初始高度 z 被限制在 0.30m 到 0.60m

```text
{
关键内容:
如果配置了 [init_state].pos，必须是长度为 3 的数值列表，其中 pos[2] 初始高度必须在 [0.30, 0.60]m，推荐 0.35m。

解读:
这防止机器人出生太低穿地或太高掉落。Go2 默认高度大约 0.4m，用户调起始姿态时不要乱改 z。

具体代码:
if not isinstance(pos, list) or len(pos) != 3:
    errors.append("[init_state] pos must be an array of length 3")

if not (0.30 <= init_height_z <= 0.60):
    errors.append(... "recommended: 0.35m")

引用:
来源文件：tools/config_checker.py
函数：_validate_init_state_params
}
```

### 166. [terrain] 通用限制：mode、num_rows、num_cols、difficulty_range、max_init_terrain_level

```text
{
关键内容:
terrain.mode 只能是 standard 或 track；num_rows 限制 [1,10]，num_cols 限制 [1,40]；difficulty_range 两端必须在 [0,1]；max_init_terrain_level 必须是 [0,9] 且小于 num_rows。

解读:
这些就是用户 TOML 的硬边界。max_init_terrain_level 用来限制初始难度档，配置得比 num_rows 还大没有意义，所以会报错。

具体代码:
valid_modes = {"standard", "track"}
elif not (1 <= num_rows <= 10):
elif not (1 <= num_cols <= 40):
if dr[0] < 0.0 or dr[0] > 1.0:
elif not (0 <= level <= 9):
if level >= num_rows_val:
    errors.append(...)

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 167. [terrain].level 是评估专用字段，不能出现在训练配置

```text
{
关键内容:
terrain.level 只能在 eval 配置使用，必须是非空整数列表，每个 level 在 [0,9]，不能重复，且不能和 num_rows 同时设置。

解读:
level 用来指定评估哪些难度档，不是训练 curriculum 参数。训练时想控制初始难度应看 max_init_terrain_level，而不是写 level。

具体代码:
if "level" in terrain_conf:
    if not is_eval:
        errors.append("[terrain] 'level' is eval-only...")
    if len(set(level_list)) != len(level_list):
        errors.append(...)
    if "num_rows" in terrain_conf:
        errors.append("[terrain] 'level' and 'num_rows' cannot both be set")

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 168. standard 模式允许的地形类型有限，不能使用 open_entry_maze

```text
{
关键内容:
standard 模式只允许 pyramid_slope、pyramid_slope_inv、pyramid_stairs、pyramid_stairs_inv、maze。nav_maze 和 open_entry_maze 被明确判定为 track-only。

解读:
如果想在普通 Standard grid 里训练 open_entry_maze，会被配置校验拒绝。open_entry_maze 是为 Track 段间衔接设计的。

具体代码:
valid_terrain_types = {
    "pyramid_slope",
    "pyramid_slope_inv",
    "pyramid_stairs",
    "pyramid_stairs_inv",
    "maze",
}
track_only_terrains = {"nav_maze", "open_entry_maze"}

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 169. standard 训练模式下子地形 proportion 之和必须为 1.0

```text
{
关键内容:
standard_conf 中各合法地形如果使用 dict + proportion 形式，所有 proportion 必须非负并且总和等于 1.0。

解读:
这决定 Standard 训练中列如何按比例分配地形类型。比例不满或超过 1 会直接报错，不会自动归一化。

具体代码:
if isinstance(terrain_value, dict) and "proportion" in terrain_value:
    total += p

if abs(total - 1.0) > 1e-6:
    errors.append("[terrain.standard] sub-terrain proportions must sum to 1.0")

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 170. standard 评估模式支持 sub_terrains 列表形式

```text
{
关键内容:
standard eval 可以用 [terrain.standard].sub_terrains 列表指定要评估的地形组合；该列表训练模式禁用，元素不能重复，且不能和 num_cols 同时设置。

解读:
这是为了做确定性评估：level 列表决定难度档，sub_terrains 列表决定地形类型，平台自动推导 grid 尺寸。训练仍应使用 proportion。

具体代码:
if "sub_terrains" in standard_conf:
    if not is_eval:
        errors.append("'sub_terrains' list form is eval-only")
    if "num_cols" in terrain_conf:
        errors.append("'sub_terrains' and 'num_cols' cannot both be set")

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 171. track 模式配置必须以 open_entry_maze 结尾

```text
{
关键内容:
track 模式如果配置 track_length 和 sub_terrains，它们必须同时出现；track_length 必须在 [1,5]；sub_terrains 长度必须等于 track_length，不能重复，最后一个必须是 open_entry_maze。

解读:
这把 Track 任务固定成“前面若干运动地形，最后以开放入口迷宫作为终点”。如果最后不是 open_entry_maze，会直接校验失败。

具体代码:
if len(sub_terrains) != track_length:
    errors.append(...)

if duplicates:
    errors.append(...)

if sub_terrains and sub_terrains[-1] != "open_entry_maze":
    errors.append("[terrain.track] sub_terrains last element must be 'open_entry_maze'")

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 172. track 模式不支持 sub_terrains_random

```text
{
关键内容:
只要 [terrain.track] 里出现 sub_terrains_random，就会报错。

解读:
这和 TrackTerrainGenerator 代码中支持 random 分支并不矛盾：平台用户配置层面禁止开放这个选项。比赛配置希望 Track 序列可控、固定，而不是用户随机拼接。

具体代码:
if "sub_terrains_random" in track_conf:
    errors.append("[terrain.track] sub_terrains_random is not supported")

引用:
来源文件：tools/config_checker.py
函数：_validate_terrain_params
}
```

### 173. domain_rand 配置限制摩擦、推力扰动等参数范围

```text
{
关键内容:
domain_rand 中 enable_domain_rand、randomize_friction、push_robots 必须是 bool；friction_range 两端必须在 [0,10]；push_interval_s > 0；max_push_vel_xy >= 0。

解读:
这说明平台允许用户开关域随机化和推机器人扰动，但参数不能离谱。过大摩擦或负 push 速度会被配置层拦截。

具体代码:
errors.extend(_validate_range_pair_within(fr, "domain_rand", "friction_range", 0.0, 10.0))
elif pis <= 0:
    errors.append(...)
elif mpv < 0:
    errors.append(...)

引用:
来源文件：tools/config_checker.py
函数：_validate_domain_rand_params
}
```

### 174. command 范围被限制在 [-10, 10]，重采样时间在 [0, 300] 秒

```text
{
关键内容:
commands.resampling_time 必须是 [min,max] 且两端在 [0,300]；commands.ranges 和 commands.limit 里的 lin_vel_x、lin_vel_y、ang_vel_z、ang_vel_yaw 都必须在 [-10,10]。

解读:
这是命令采样配置的安全边界。实际 Go2 训练一般远小于这个范围；写到极端值虽然可能通过校验，但未必可学。

具体代码:
_validate_range_pair_within(commands_conf["resampling_time"], ..., 0.0, 300.0)

for section_key in ["ranges", "limit"]:
    velocity_keys = ["lin_vel_x", "lin_vel_y", "ang_vel_z", "ang_vel_yaw"]
    _validate_range_pair_within(..., -10.0, 10.0)

引用:
来源文件：tools/config_checker.py
函数：_validate_commands_params
}
```

### 175. reward 配置只允许 weight 和 params，weight 必须是数字

```text
{
关键内容:
训练配置里的每个 rewards.<name> 必须是 dict/table，必须有 numeric weight，只允许 weight 和 params 两个字段；params 如果存在也必须是 dict。

解读:
这对应反射式 reward 系统：TOML 中的 reward 名会映射到代码中的 reward 方法。拼错字段或把 weight 写成字符串会直接被拒绝。

具体代码:
if "weight" not in reward_value:
    errors.append(...)
if not _is_numeric(weight):
    errors.append(...)
for key in reward_value:
    if key not in ("weight", "params"):
        errors.append(...)

引用:
来源文件：tools/config_checker.py
函数：_validate_rewards_params
}
```

### 176. reward params 中常见数值参数禁止写 bool 或字符串

```text
{
关键内容:
std、threshold、obstacle_threshold、trapped_threshold、tanh_mult、target_height、velocity_threshold、stand_still_scale、goal_reached_threshold 等参数必须是数字，bool 和 string 都不允许。

解读:
Python 里 bool 是 int 的子类，如果不拦截 True/False 可能被当作 1/0 静默污染 reward。这里显式禁止，是为了避免调参时出现很难发现的错误。

具体代码:
numeric_param_keys = {
    "std",
    "threshold",
    "obstacle_threshold",
    "trapped_threshold",
    ...
}
if p_key in numeric_param_keys and not _is_numeric(p_val):
    errors.append(...)

引用:
来源文件：tools/config_checker.py
函数：_validate_rewards_params
}
```

### 177. img2mp4 是评估/可视化辅助工具，不影响训练策略

```text
{
关键内容:
img2mp4 会把目录里的 png/jpg 按顺序合成 1920x1080 MP4，图片少于等于 50 张时视频 5 秒，少于等于 200 张时 10 秒，否则 20 秒，编码使用 mp4v。

解读:
这是视频生成工具，通常用于评估截图转视频或可视化，不参与仿真、reward、score 计算。

具体代码:
output_size = (1920, 1080)
if total_image_files_count <= 50:
    video_duration = 5
elif total_image_files_count <= 200:
    video_duration = 10
else:
    video_duration = 20
fps = total_image_files_count / video_duration
fourcc = cv2.VideoWriter_fourcc(*"mp4v")

引用:
来源文件：tools/utils.py
函数：img2mp4
}
```

### 178. ensure_empty_directory 会清空目录内容，使用时要谨慎

```text
{
关键内容:
ensure_empty_directory(path) 如果目录存在，会删除里面所有文件、链接和子目录；如果不存在，则创建目录。

解读:
这是一个清理输出目录的工具。它很方便，但不要把模型目录、日志目录或工作目录误传进去，否则会删掉已有内容。

具体代码:
if os.path.exists(path):
    for entry in os.listdir(path):
        if os.path.isfile(entry_path) or os.path.islink(entry_path):
            os.remove(entry_path)
        else:
            shutil.rmtree(entry_path)
else:
    os.makedirs(path, exist_ok=True)

引用:
来源文件：tools/utils.py
函数：ensure_empty_directory
}
```

### 179. tools/utils.py 里有一套旧版地形统计打分公式

```text
{
关键内容:
calculate_scores 使用 SUCCESS_WEIGHT=500、SPEED_WEIGHT=300、STABILITY_WEIGHT=200，公式是 success/count*500 + sigmoid(avg_velocity)*300 + exp(avg_stability*1000)*200。

解读:
这和前面 base_scorer 的官方链条很像，但这个工具函数更像监控/旧版统计辅助。它接受 episode_success、episode_velocity、episode_stability 的聚合值，不负责从仿真原始状态计算这些量。

具体代码:
SUCCESS_WEIGHT = 500
SPEED_WEIGHT = 300
STABILITY_WEIGHT = 200

success_score = (success / count) * SUCCESS_WEIGHT
speed_score = sigmoid(avg_velocity) * SPEED_WEIGHT
stability_score = math.exp(avg_stability * 1000) * STABILITY_WEIGHT

引用:
来源文件：tools/utils.py
函数：calculate_scores
}
```

### 180. calculate_scores 对 NaN/Inf 和 count=0 会直接返回 0 分

```text
{
关键内容:
如果 count 为 0，或者 success、velocity、stability、count 中任何值是 NaN/Inf，calculate_scores 返回四个 0.0。

解读:
这避免监控统计被异常值污染。若训练面板某个地形分数突然全 0，除了策略失败，也要检查是否有 NaN/Inf 或样本数为 0。

具体代码:
if (
    count == 0
    or any(math.isnan(x) for x in [success, velocity, stability, count])
    or any(math.isinf(x) for x in [success, velocity, stability, count])
):
    return (0.0, 0.0, 0.0, 0.0)

引用:
来源文件：tools/utils.py
函数：calculate_scores
}
```

### 181. calculate_terrain_stats 使用的是旧版 terrain_name_map

```text
{
关键内容:
calculate_terrain_stats 里的 terrain_name_map 是 ripples、slope、stairs_up、stairs_down、obstacles、composite1、composite2、flat，不是新一代 standard/track 的 pyramid_slope、maze、open_entry_maze。

解读:
这段更像旧版统计兼容代码。新一代 Unitree terrain 命名和这里不完全一致，所以不要直接用它判断当前 standard/track 的地形名称，除非平台仍在某个旧监控路径里转换了 terrain code。

具体代码:
terrain_name_map = {
    0: "ripples",
    1: "slope",
    2: "stairs_up",
    3: "stairs_down",
    4: "obstacles",
    5: "composite1",
    6: "composite2",
    7: "flat",
}

引用:
来源文件：tools/utils.py
函数：calculate_terrain_stats
}
```

### 182. calculate_terrain_stats 按 terrain type 和 level 生成每级指标

```text
{
关键内容:
calculate_terrain_stats 会根据 terrain_types、terrain_level，把每个 episode 的 success/velocity/stability 分数组合到 terrain_name_score_level0..9 等 key 里。

解读:
这是监控统计思路：每种地形、每个难度 level 单独看分数。它有助于定位“低难度能过，高难度不过”或“某类地形专门失败”的问题。

具体代码:
for level in range(10):
    result[f"{terrain_name}_score_level{level}"] = single_score
    result[f"{terrain_name}_avg_episode_stability_level{level}"] = stab_avg
    result[f"{terrain_name}_avg_episode_success_level{level}"] = succ_avg
    result[f"{terrain_name}_avg_episode_velocity_level{level}"] = vel_avg

引用:
来源文件：tools/utils.py
函数：calculate_terrain_stats
}
```

### 183. calculate_terrain_stats 用 terrain_proportions 推断 terrain type 名称

```text
{
关键内容:
函数从 usr_conf["terrain"]["terrain_proportions"] 和 hidden_terrain_proportions 计算 cumulative_proportions，再用 t_code / num_cols + 0.001 反推出地形类型。

解读:
这是按列比例分配地形的旧式统计方式。它假设 terrain_types 是列号，并且列号能通过比例映射回地形名。Track 模式 col=难度档时，不应直接套这个逻辑。

具体代码:
terrain_proportions = usr_conf["terrain"]["terrain_proportions"]
hidden_terrain_proportions = usr_conf["terrain"].get("hidden_terrain_proportions", [0.0, 0.0])
choice = t_code / num_cols + 0.001
terrain_name = terrain_name_map.get(get_terrain_index(cumulative_proportions, choice))

引用:
来源文件：tools/utils.py
函数：calculate_terrain_stats
}
```

### 184. sort_by_terrain_map_order 也是旧版 terrain map 的排序辅助

```text
{
关键内容:
sort_by_terrain_map_order 按 terrain_name_map 的 key 顺序排序，详细数据还会按 level 升序排。

解读:
它只影响统计输出顺序，不影响训练、评估、地形生成或分数计算。由于依赖旧 terrain_name_map，新一代地形展示时要谨慎复用。

具体代码:
terrain_order = {t: idx for idx, t in enumerate(terrain_name_map.keys())}
return sorted(data_items, key=get_sort_key)

引用:
来源文件：tools/utils.py
函数：sort_by_terrain_map_order
}
```

## 第八批：KaiwuDRL 底层配置、服务发现与进程编排

### 185. common_python 是框架底座，不是 Isaac Sim 物理环境本身

```text
{
关键内容:
common_python 目录里的 alloc、config、exception、logging、monitor、utils 等模块是 KaiwuDRL 运行框架公共库，负责配置、日志、服务发现、监控和进程辅助。

解读:
参赛调策略时不需要深入修改这些模块。真正决定 Isaac Sim 怎么生成环境、怎么 reset、reward 怎么算、obs/action 是什么的代码，仍然在 tools/base_env、unitree_rl_lab、agent_ppo/feature、agent_ppo/workflow 这些层。common_python 更像“把训练系统跑起来”的底层胶水。

具体代码:
common_python/
  alloc/
  config/
  exception/
  logging/
  monitor/
  utils/

引用:
来源目录：common_python
}
```

### 186. AllocProxy 是服务注册进程，周期性向 alloc 注册当前 aisrv/actor/learner

```text
{
关键内容:
AllocProxy 继承 multiprocessing.Process，启动后解析配置、初始化日志、创建 AllocUtils，并周期性调用 alloc_interact 注册服务。

解读:
这是分布式训练用的服务发现辅助进程。它不参与物理仿真、神经网络前向或 PPO 更新，只是让 aisrv、actor、learner、env 这些进程能互相找到地址。

具体代码:
class AllocProxy(multiprocessing.Process):
    def before_run(self):
        self.alloc_util = AllocUtils(self.logger)
        self.alloc_interact()
        self.set_event_alloc_interact()

    def alloc_interact(self):
        code, msg = self.alloc_util.registry()

引用:
来源文件：common_python/alloc/alloc_proxy.py
类：AllocProxy
}
```

### 187. alloc 注册接口是 /api/registry，查询实例接口是 /api/get

```text
{
关键内容:
AllocUtils.registry 向 alloc_addr/api/registry POST 当前进程的 set、role、addr、assign_limit、task_id。AllocUtils.get 向 alloc_addr/api/get 请求目标 role 的地址。

解读:
这解释了云端分布式为什么不需要你手写 IP：每个角色先注册自己，再通过 alloc 服务查询别人。单机训练或本地 train_test 时通常不需要关心这层，除非出现 actor/learner 连不上、alloc 注册失败等框架问题。

具体代码:
url = f"{self.alloc_addr}/api/registry"
resp = http_utils_post(url, self.post_param)

url = f"{self.alloc_addr}/api/get"
resp = http_utils_post(url, self.get_param)

引用:
来源文件：common_python/alloc/alloc_utils.py
类：AllocUtils
}
```

### 188. KaiwuDRL 的角色包括 aisrv、actor、learner、client、env

```text
{
关键内容:
AllocUtils 中 SERVER_ROLE_CONFIGURE 和 SERVER_PORT_CONFIGURE 把 aisrv、actor、learner、client、kaiwu_env 映射成 alloc role 和端口。

解读:
这是 Kaiwu 分布式架构的角色层。大致理解即可：aisrv/env 负责和环境交互，actor/learner 负责策略推理与学习，alloc 只负责发现地址。Isaac Sim 的 ManagerBasedRLEnv 是被上层 workflow/环境封装调用，不是在 alloc 里跑。

具体代码:
self.SERVER_ROLE_CONFIGURE = {
    CommonDefine.SERVER_AISRV: int(CONFIG.alloc_process_role_aisrv),
    CommonDefine.SERVER_ACTOR: int(CONFIG.alloc_process_role_actor),
    CommonDefine.SERVER_LEARNER: int(CONFIG.alloc_process_role_learner),
    CommonDefine.SERVER_CLIENT: int(CONFIG.alloc_process_role_client),
    CommonDefine.SERVER_ENV: int(CONFIG.alloc_process_role_kaiwu_env),
}

引用:
来源文件：common_python/alloc/alloc_utils.py
类：AllocUtils
}
```

### 189. aisrv 会通过 alloc 获取 actor 和 learner 地址

```text
{
关键内容:
get_actor_learner_ip 会分别请求 actor 和 learner 地址；非 self_play 模式下返回 actor_address、learner_address；self_play 模式下会区分当前 set 和旧 set。

解读:
这属于分布式 Actor-Learner 通信。策略训练时如果 sample 不消费、actor 连接失败、learner 地址为空，可能要查这里；普通算法调参一般不改。

具体代码:
code, msg, content = self.get(CommonDefine.SERVER_ACTOR, set_name, self_play_set_name)
...
code, msg, content = self.get(CommonDefine.SERVER_LEARNER, set_name, self_play_set_name)

return actor_address, learner_address, None, None

引用:
来源文件：common_python/alloc/alloc_utils.py
函数：get_actor_learner_ip
}
```

### 190. ConfigControl 是全局 CONFIG 的来源，解析顺序是系统配置、用户配置、环境变量

```text
{
关键内容:
ConfigControl.parse_configure 先解析系统配置文件，再解析传入的业务配置文件，最后 read_from_env 用环境变量覆盖内存中的 CONFIG 字段。

解读:
这解释了为什么同一个 TOML 在云端和本地可能行为不同：容器环境变量可以覆盖配置。查运行参数时不能只看文件，也要考虑环境变量。

具体代码:
if system_names and configure_system_file:
    self.load_data_from_config(system_name, self.config_system_file)

for name in names:
    self.load_data_from_config(name, self.config_file)

self.read_from_env()

引用:
来源文件：common_python/config/config_control.py
类：ConfigControl
}
```

### 191. configure_app.toml 是用户可影响 KaiwuDRL 的关键全局配置之一

```text
{
关键内容:
ConfigControl 注释写明：用户修改 app 下的配置，其中 configure_app.toml 的修改可以影响 KaiwuDRL；kaiwudrl 下的是框架内部配置。

解读:
比赛里通常应该改 conf/configure_app.toml、agent_ppo/conf/*.toml、算法代码，而不是 common_python 或 kaiwudrl 内部配置。后者属于平台底座，改了容易破坏运行。

具体代码:
目录文件规则:
1. 用户修改: app下的, 其中configure_app.toml的修改可以影响到KaiwuDRL
2. KaiwuDRL修改: kaiwudrl下的, 在对外时可以进行隐藏

引用:
来源文件：common_python/config/config_control.py
类：ConfigControl
}
```

### 192. parse_main_configure 会加载 project_default、检查 app 配置范围，再加载 app 配置

```text
{
关键内容:
parse_main_configure 加载 main 配置后，会 parse_project_default_configure、check_app_configure、parse_app_configure，最后再读取环境变量。

解读:
项目默认配置、用户 app 配置、环境变量会叠加形成最终 CONFIG。check_app_configure 是软校验：如果 configure_app.toml 出现不在项目允许范围内的 key，会打印提醒。

具体代码:
self.load_data_from_config("main", self.config_system_file)
self.parse_project_default_configure()
self.check_app_configure()
self.parse_app_configure()
self.read_from_env()

引用:
来源文件：common_python/config/config_control.py
函数：parse_main_configure
}
```

### 193. ConfigControl 缺失属性会返回默认字符串，可能掩盖拼写错误

```text
{
关键内容:
ConfigControl.__getattr__ 在属性不存在时返回 CommonDefine.CONFIG_DEFAULT_STRING，而不是直接抛 AttributeError。

解读:
这让框架更容错，但也意味着配置 key 拼错时可能不会立刻崩，而是得到默认字符串。排查配置不生效时，要优先检查 key 名是否拼对。

具体代码:
def __getattr__(self, name):
    try:
        return object.__getattribute__(self, name)
    except AttributeError:
        return CommonDefine.CONFIG_DEFAULT_STRING

引用:
来源文件：common_python/config/config_control.py
类：ConfigControl
}
```

### 194. exception_handler 是通用异常捕获装饰器，但不是 Isaac Sim 运行核心

```text
{
关键内容:
exception_handler 用装饰器捕获函数异常，记录函数名、异常信息和 traceback，可附加 extra_message。

解读:
这是公共工具。值得注意的是代码注释说 logger=None 时用默认 logging，但实现里 used_logger = logger，若没有传 logger 且真的发生异常，used_logger.error 本身可能报错。除非你要使用这个装饰器，否则不影响训练主链路。

具体代码:
def exception_handler(*expected_exceptions, logger=None, extra_message=None):
    used_logger = logger
    ...
    except expected_exceptions or Exception as e:
        error_msg = f"function {func.__name__} execution failed..."
        used_logger.error(error_msg)

引用:
来源文件：common_python/exception/exception.py
函数：exception_handler
}
```

### 195. Isaac Sim “怎么跑”不在这批代码里，而在环境注册、workflow 和 base_env 链路里

```text
{
关键内容:
这批 common_python 代码负责让进程、配置、服务发现和日志运转；Isaac Sim 真正启动环境的链路是 gym 注册的 Unitree-Go2-Velocity -> parse_env_cfg/load_cfg_from_registry -> ManagerBasedRLEnv -> tools/base_env 封装 -> workflow 调用 reset/step。

解读:
如果目标是提升比赛成绩，优先看：观测处理、reward、env_cfg、terrain、reset、PPO workflow、模型结构。只有当训练任务起不来、服务连不上、配置被覆盖、监控异常时，才需要深入 common_python。

具体代码:
gym.register(id="Unitree-Go2-Velocity", entry_point="isaaclab.envs:ManagerBasedRLEnv", ...)
cfg = load_cfg_from_registry(task_name, entry_point_key)
env.step(action)

引用:
来源文件：unitree_rl_lab/tasks/locomotion/robots/go2/__init__.py
来源文件：unitree_rl_lab/utils/parse_cfg.py
来源文件：tools/base_env.py
来源文件：common_python/*
}
```
