# 奖励函数升级整理方案

本文档整理当前奖励函数升级方向，目标是把奖励从“很多辅助约束堆叠”调整为“速度、完成、评分拟合、安全底线”四条清晰主线。

核心目标：

- 让模型真正追速度，不再满足于慢走或原地跳。
- 让模型优先完成任务，减少绕圈、卡墙、超时。
- 姿态和电量尽量直接对齐官方评分函数。
- 减少限制跑跳动作空间的重复辅助项，只保留必要安全底线。

## 1. 总体奖励结构

最终奖励建议分成四层：

```text
第一层：完成任务
到达目标、靠近目标、避免死亡、避免超时、减少卡住。

第二层：速度推进
低于命令速度惩罚，达到命令速度奖励，超过命令速度有限奖励。

第三层：评分拟合
姿态用 pose_score_formula，电量用 energy_score_formula。

第四层：安全底线
保留少量关节限位、非脚部接触、动作平滑等防崩坏项。
```

一句话：

```text
先让它快、能到、不死，再用官方姿态/电量公式把动作收干净。
```

## 2. 速度奖励升级

当前 `track_lin_vel_xy` 更像“接近命令速度就好”。  
新的目标不是只接近，而是让速度奖励具有方向性：

```text
实际速度 < 命令速度：
  给惩罚，越慢惩罚越大。

实际速度 ≈ 命令速度：
  给高奖励。

实际速度 > 命令速度：
  给额外奖励，但收益递减。

实际速度过高：
  不无限加分，交给姿态、电量、死亡、接触惩罚制衡。
```

建议把速度奖励改造成类似 sigmoid / speed band 的形状：

```text
低速区：强惩罚
达标区：快速涨分
超速区：小幅奖励
过快区：奖励饱和或下降
```

人话：

```text
不要奖励慢走。
允许它冲。
但冲得太乱、太费电、太容易死，不应该无限拿分。
```

## 3. track_lin_vel_xy 与 command_speed_advantage

建议让这两项成为速度主线。

### track_lin_vel_xy

作用：

```text
让实际 vx/vy 跟随环境发出的速度命令。
```

升级方向：

```text
从“只看误差大小”改成“低于目标惩罚，达到目标奖励，略高于目标也奖励”。
```

注意：

```text
如果官方速度限制是 0.8，那么命令速度可以固定到 0.8。
奖励可以鼓励实际速度超过 0.8，但不要再去 base_env 或 reward 里强行覆盖环境 command。
```

### command_speed_advantage

作用：

```text
实际 vx 高于 cmd_vx 给奖励，低于 cmd_vx 给惩罚。
```

升级方向：

```text
加大低速惩罚。
保留超速奖励。
超速奖励设置上限，避免姿态、电量彻底崩掉。
```

## 4. forward_heading_velocity

当前配置大概是：

```text
weight = 0.72
target_speed = 0.58
max_reward = 1.0
```

它的作用是奖励机身朝自身前方移动。

问题：

```text
target_speed = 0.58 偏低。
如果模型拿到 0.58 左右就能获得较高奖励，它可能不愿意继续追 0.8 或更高实际速度。
```

建议：

```text
降低或去掉 forward_heading_velocity。
把速度压力集中到 track_lin_vel_xy 和 command_speed_advantage。
```

人话：

```text
不要再给一个低速前进目标单独发奖，否则它会学“够用就慢跑”。
```

## 5. 姿态与电量评分拟合

姿态主奖励：

```text
pose_score_formula
```

电量主奖励：

```text
energy_score_formula
```

这两个比间接辅助项更接近官方评分。

### pose_score_formula

作用：

```text
根据 roll / pitch 偏差计算姿态分。
身体越平，奖励越高。
```

建议：

```text
提高权重，作为主姿态奖励。
```

### energy_score_formula

作用：

```text
根据 power = abs(torque * joint_vel) 近似官方能耗分。
能耗越低，奖励越高。
```

建议：

```text
中等权重。
不能太高，否则模型可能为了省电变慢、不动、少动作。
```

### 为什么不建议保留很多重复姿态/电量项

如果同时保留太多间接项：

```text
lin_vel_z
ang_vel_xy
correct_base_height
flat_orientation
posture_stability
raw energy
joint_torques
```

会出现目标冲突：

```text
速度奖励希望它跑和跳；
高度/角速度/平坦姿态奖励希望它稳和少动；
能耗项希望它省力；
结果可能回到慢走、原地跳、动作僵硬。
```

所以姿态和电量的主信号应尽量交给官方评分拟合项。

## 6. 可疑限制项整理

这些项可能限制跑跳和高速探索。

### lin_vel_z

作用：

```text
惩罚机身竖直速度。
```

风险：

```text
会压跳、压弹、压上下楼梯时必要的竖直运动。
```

建议：

```text
大幅降低或关闭。
```

### ang_vel_xy

作用：

```text
惩罚 roll/pitch 方向角速度。
```

风险：

```text
太重会让复杂地形动作僵硬，跑跳时不敢快速调整身体。
```

建议：

```text
降低或关闭，只依赖 pose_score_formula 做主要姿态约束。
```

### correct_base_height

作用：

```text
惩罚机身高度偏离目标高度。
```

风险：

```text
跑步、跳跃、上下楼梯时身体高度本来就需要变化。
固定高度约束过强会压制高速动作。
```

建议：

```text
降低或关闭。
```

### flat_orientation

作用：

```text
惩罚机身不水平。
```

风险：

```text
和 pose_score_formula 重复。
过强会让模型在坡、楼梯、冲刺时过于保守。
```

建议：

```text
降低。
```

### posture_stability

作用：

```text
惩罚 roll/pitch 快速变化。
```

风险：

```text
可以减少抖动，但太重会压制跑跳节奏。
```

建议：

```text
保留很小权重，或者先关闭观察。
```

### front_hip_to_default

作用：

```text
重点约束前腿髋关节接近默认角。
```

风险：

```text
限制前腿大步幅，影响跑、跳、上坡、上楼梯。
```

建议：

```text
关闭或大幅降低。
```

### joint_position_penalty

作用：

```text
惩罚全关节偏离默认姿态。
```

风险：

```text
会锁住动作空间，让模型更难探索高速步态。
```

建议：

```text
关闭或大幅降低。
```

### air_time_variance_penalty

作用：

```text
惩罚四只脚腾空时间差异过大。
```

风险：

```text
强迫步态对称，可能压制跳跃、奔跑、非对称上楼动作。
```

建议：

```text
关闭或极低权重。
```

## 7. 关节姿态与接触安全

这一组不是直接评分项，属于安全辅助。

### dof_pos_limits

作用：

```text
惩罚关节接近或超过软限位。
```

建议：

```text
保留很小权重。
```

原因：

```text
这是安全底线，防止极端关节角度钻仿真漏洞。
```

### hip_to_default

作用：

```text
约束四个髋关节接近默认角。
```

建议：

```text
弱化，但不建议完全删除。
```

原因：

```text
它能控制极端外八/内八。
完全去掉可能让姿态、电量、迁移性变差。
```

### front_hip_to_default

建议：

```text
关闭或大幅降低。
```

原因：

```text
前腿管得太死，容易影响跑跳和上楼。
```

### joint_position_penalty

建议：

```text
关闭或大幅降低。
```

原因：

```text
全关节默认姿态约束太强，会压动作空间。
```

### undesired_contacts

作用：

```text
惩罚非脚部不该发生的接触。
```

建议：

```text
保留中等权重。
```

原因：

```text
防止身体、大腿、小腿乱撞地形。
这不是姿态分辅助，而是防止训练崩坏的安全项。
```

## 8. 脚步质量奖励

这一组控制脚怎么迈、怎么落地。

### feet_swing_forward

作用：

```text
奖励摆动脚向前迈够位置。
```

建议：

```text
保留，必要时小幅提高。
```

原因：

```text
可以防止只抬脚不往前迈，尤其对楼梯边缘有帮助。
```

### feet_slide

作用：

```text
脚接触地面时，如果水平速度过大就扣分。
```

建议：

```text
保留小权重。
```

原因：

```text
减少拖脚和打滑，但太重会让它不敢快跑。
```

### feet_stumble

作用：

```text
脚受到很大水平接触力时扣分。
```

建议：

```text
保留小权重。
```

原因：

```text
减少踢墙、踢台阶边缘。
```

### air_time_variance_penalty

建议：

```text
关闭或极低。
```

原因：

```text
对称步态不一定是高速最优解，尤其跑跳和楼梯可能需要非对称动作。
```

### base_lateral_vel

作用：

```text
惩罚侧向速度。
```

建议：

```text
保留小权重。
```

原因：

```text
防止螃蟹步和侧滑，但不能太重，否则影响绕障和姿态调整。
```

## 9. score_guidance

作用：

```text
只有模型真的响应速度命令时，才额外给姿态/电量奖励。
```

大致公式：

```text
score_guidance =
是否有移动命令
× 速度跟踪分
× (姿态分 * posture_weight + 电量分 * (1 - posture_weight))
```

它解决的问题：

```text
如果单独奖励姿态和电量，模型可能站着不动骗高分。
score_guidance 要求它先动起来、先跟速度，再谈姿态和省电。
```

建议：

```text
保留小权重即可。
不要作为主奖励。
```

原因：

```text
当前已经有 track_lin_vel_xy、command_speed_advantage、pose_score_formula、energy_score_formula。
score_guidance 只是把这些目标轻轻绑定在一起。
```

## 10. 完成、死亡、超时

这一组应该是训练主轴之一。

建议强化：

```text
task_complete
reach_goal
approach_goal
goal_velocity_projection
navigation_time
stuck_penalty
termination
```

### task_complete

作用：

```text
完成整条 track 的最终奖励。
```

建议：

```text
高权重。
```

### reach_goal

作用：

```text
接近目标点时给稀疏奖励。
```

建议：

```text
中高权重。
```

### approach_goal

作用：

```text
每一步靠近目标就给奖励。
```

建议：

```text
保留。
```

### goal_velocity_projection

作用：

```text
奖励速度朝目标方向投影。
```

建议：

```text
保留。
```

### navigation_time

作用：

```text
每一步时间惩罚。
```

建议：

```text
适当加重。
```

原因：

```text
减少慢走、绕圈、拖时间。
```

### termination

作用：

```text
死亡或异常终止惩罚。
```

建议：

```text
加重，但不要重到开局探索全崩。
```

### stuck_penalty

作用：

```text
惩罚卡住、原地跳、几乎不移动。
```

建议：

```text
保留。
```

## 11. 推荐保留列表

建议主保留：

```text
track_lin_vel_xy
command_speed_advantage
pose_score_formula
energy_score_formula
task_complete
reach_goal
approach_goal
goal_velocity_projection
navigation_time
termination
stuck_penalty
undesired_contacts
dof_pos_limits
action_smoothness 或 joint_acc
feet_swing_forward
feet_slide
feet_stumble
base_lateral_vel
hip_to_default 小权重
score_guidance 小权重
```

## 12. 建议弱化列表

建议降低权重：

```text
flat_orientation
posture_stability
hip_to_default
dof_pos_limits
feet_slide
feet_stumble
base_lateral_vel
action_smoothness
joint_acc
```

## 13. 建议关闭或大幅降低列表

建议关闭或大幅降低：

```text
forward_heading_velocity
lin_vel_z
ang_vel_xy
correct_base_height
raw energy
joint_torques
front_hip_to_default
joint_position_penalty
stand_still_motion
commanded_still_penalty
air_time_variance_penalty
```

## 14. 推荐最终奖励主线

最终奖励可以简化成：

```text
速度：
  track_lin_vel_xy
  command_speed_advantage
  可选 asymmetric_speed_band

完成：
  approach_goal
  goal_velocity_projection
  reach_goal
  task_complete
  navigation_time
  stuck_penalty

评分：
  pose_score_formula
  energy_score_formula
  score_guidance 小权重

安全：
  termination
  undesired_contacts
  dof_pos_limits 小权重
  action_smoothness 或 joint_acc 小权重
  feet_slide 小权重
  feet_stumble 小权重
```

## 15. 风险提醒

### 风险 1：姿态/电量太高会变慢

如果 `pose_score_formula` 和 `energy_score_formula` 太高，模型可能更愿意慢走、省电、少动。

解决：

```text
速度奖励和时间惩罚必须足够强。
能耗奖励不要压过完成和速度。
```

### 风险 2：安全项全删会学歪

如果把接触、关节限位、动作平滑全部删掉，模型可能短期跑得更猛，但容易学出：

```text
极端关节角
身体蹭地
大腿小腿撞台阶
高频抖动
开局死亡
```

解决：

```text
只删强约束，保留安全底线。
```

### 风险 3：速度奖励太极端会只跳不跑

如果只加大速度奖励，模型可能学会乱跳、乱冲，但实际推进不稳定。

解决：

```text
速度奖励要和 goal_velocity_projection、approach_goal、feet_swing_forward、stuck_penalty 配合。
```

## 16. 最终结论

这次升级的重点不是继续堆奖励，而是：

```text
减少冲突项。
强化速度和完成。
姿态/电量直接对齐官方评分。
只保留必要安全底线。
```

最终希望模型学到的行为是：

```text
看到命令就主动提速；
直线、坡、楼梯都尽量向目标推进；
迷宫里减少乱撞和原地跳；
完成优先，时间越短越好；
在速度足够的基础上，再优化姿态和电量。
```

