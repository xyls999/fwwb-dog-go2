# 图3-1 非对称 Actor-Critic 网络架构

本项目采用非对称 Actor-Critic 架构。Actor 面向实际执行，负责输出关节动作；Critic 面向训练优化，负责估计状态价值。

Actor 网络采用三层全连接 MLP。基础运控阶段输入为 301 维，由 45 维本体感知特征和 256 维高度扫描特征组成；Track 导航阶段扩展为 304 维，新增目标相对位置 `x`、目标相对位置 `y` 和归一化目标距离 3 维目标感知特征。隐藏层规模为 512、256、128，激活函数采用 ELU，输出为 12 维关节动作均值。动作分布采用高斯分布，标准差作为可学习参数参与采样，Track 阶段初始噪声标准差设为 0.80，以兼顾探索能力与步态稳定性。

Critic 网络同样采用三层 MLP。基础阶段输入为 316 维特权观测，相比 Actor 引入更丰富的训练期状态信息；Track 阶段扩展为 319 维，同步加入 3 维目标感知特征。隐藏层规模同样为 512、256、128，并在中间层加入 LayerNorm，以降低复杂地形和多目标奖励造成的价值估计波动。输出层为 1 维状态价值，用于 PPO 优势函数计算和策略更新。

该结构核心在于实现策略执行与价值估计的信息解耦。Actor 仅保留部署阶段可直接获取的紧凑观测输入，从而保证策略推理的实时性与可迁移性；Critic 则在训练阶段引入更充分的特权状态信息，以提升状态价值函数的表达能力和优势估计的稳定性。Track 导航阶段进一步引入 3 维目标条件特征，使策略能够显式建模机器人自身状态、局部地形信息与全局目标方位之间的关联关系，从而将原有的局部运动控制策略扩展为具备目标条件约束的导航控制策略。


关键改进:在保持原有网络主体结构不变的基础上，为 Actor 与 Critic 同步增加 3 维目标感知特征，实现从基础运动控制到 Track 目标导航的能力扩展。该改动使策略由传统的局部反应式运动控制，进一步转变为目标条件化控制，即策略输出不仅由当前本体状态和局部地形决定，还受到终点相对方位和距离的显式约束。

从训练机制上看，新增目标特征改善了长时序 Track 任务中的信用分配问题。对于赛道导航任务，仅依靠终点完成奖励往往存在稀疏性强、反馈延迟长的问题；引入目标相对位置和距离后，Actor 能够获得更明确的方向性输入，Critic 也能够更准确地区分不同任务阶段的状态价值，从而提升 PPO 更新时优势估计的有效性。

同时，本项目在 Critic 中引入 LayerNorm，增强价值网络对不同地形、不同速度阶段和不同奖励尺度的适应能力。该设计有助于抑制复杂地形下价值函数震荡，提高训练过程的数值稳定性，减少策略在后期微调中对已有稳定步态的破坏。

总体而言，该网络改造具有低侵入、高迁移价值的特点：一方面，Actor 仅增加少量目标感知维度，不显著增加推理计算量；另一方面，Critic 在训练阶段利用更丰富的信息提升价值估计质量，使模型能够在保留原有运控能力的基础上，逐步获得面向 Track 赛道的目标导航能力。该设计兼顾了工程可实现性、训练稳定性和任务适应性。