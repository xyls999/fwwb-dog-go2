# 四足机器人强化学习系统工程架构设计

本项目面向 Unitree Go2 四足机器人复杂赛道导航任务，构建了一套基于 KaiwuDRL 与 Isaac Lab 的强化学习训练系统。系统围绕复杂地形自主导航、高质量运动控制、评分函数对齐优化三个核心目标展开，通过目标感知观测、非对称 Actor-Critic 网络、地形自适应速度锚点、评分对齐奖励函数和监控诊断闭环，实现从基础运动控制到 Track 赛道自主导航的能力迁移。

整体架构不是单一 PPO 策略训练，而是由环境交互、观测增强、策略学习、奖励建模、训练稳定性控制和监控反馈共同组成的系统工程。

```text
Isaac Lab 仿真环境
    ↓
KaiwuDRL 训练工作流
    ↓
目标感知观测与地形语义增强
    ↓
非对称 Actor-Critic 策略网络
    ↓
评分对齐奖励函数
    ↓
PPO 稳定优化
    ↓
监控诊断与参数迭代
```

## 基于 KaiwuDRL-Isaac Lab 协同的训练系统架构

系统底层由 Isaac Lab 提供高保真四足机器人动力学仿真，KaiwuDRL 负责分布式训练调度、样本采集、策略更新与模型保存。项目通过 `isaac_env` 环境封装层，将 TOML 配置、地形生成、速度命令、奖励注册、评分统计和视频记录统一接入训练流程。

该设计实现了训练环境与算法模块的解耦：环境侧负责物理交互与任务评估，智能体侧负责观测处理、策略决策和 PPO 更新。通过该架构，系统能够支持 4096 并行环境的大规模采样，提高训练样本吞吐量与策略更新稳定性。

**重点亮点**

- 支持 Track 赛道模式下的复杂地形串联训练。
- 通过 TOML 配置集中管理地形、速度、奖励和训练参数。
- 训练环境、奖励函数、模型结构与 PPO 优化过程模块化解耦，便于快速迭代。

## 基于目标感知观测的 Track 导航架构

传统四足机器人运动控制通常依赖本体状态与局部高度扫描，策略只能学习如何走，但缺乏往哪里走的显式信息。针对 Track 赛道终点导航需求，本项目在观测空间中新增目标感知特征，将终点位置转换到机器人自身坐标系下，并加入目标距离信息。

当前观测结构如下：

| 网络 | 基础维度 | Track 阶段维度 | 新增信息 |
| --- | ---: | ---: | --- |
| Actor | 301 | 304 | 目标相对 x、目标相对 y、目标距离 |
| Critic | 316 | 319 | 目标相对 x、目标相对 y、目标距离 |

该设计使策略能够直接感知终点相对自身的方向和距离，从而由局部运动控制扩展为目标条件化导航控制。

**创新点**

- 仅增加 3 维低成本特征，即实现从速度跟随到目标导航的能力增强。
- 目标信息采用机器人局部坐标表达，降低策略学习难度，提高导航泛化能力。
- Actor 与 Critic 同步加入目标特征，保证执行端与训练端任务语义一致。

## 基于非对称 Actor-Critic 的策略学习架构

系统采用非对称 Actor-Critic 强化学习结构。Actor 面向实际执行，只接收可部署观测；Critic 面向训练阶段，额外接收特权信息以提高价值估计精度。这种结构体现了执行轻量化、训练信息增强的设计思想。

网络结构采用三层 MLP：

| 模块 | 输入 | 输出 | 隐藏层 |
| --- | ---: | ---: | --- |
| Actor | 304 | 12 维关节动作 | 512-256-128 |
| Critic | 319 | 1 维价值估计 | 512-256-128 |

Actor 输出 12 维关节动作均值，通过高斯分布进行采样；Critic 输出状态价值，并在网络中加入 LayerNorm，提高复杂奖励分布下的价值估计稳定性。

**创新点**

- Actor 保持轻量，利于部署和迁移。
- Critic 利用特权观测提升训练效率，降低策略梯度方差。
- 标准差上下限约束可防止探索噪声过大破坏已有步态。

## 基于地形自适应速度锚点的任务控制架构

Track 赛道由坡面、楼梯和迷宫等地形串联构成，不同阶段对速度和稳定性的要求不同。系统在训练工作流中引入阶段速度锚点，根据地形识别结果为策略提供不同的速度参考。

例如：

- 坡面阶段保持较高速度，保证时间效率；
- 楼梯阶段降低速度，优先保持姿态和能耗；
- 迷宫阶段强调目标方向、避障和终点收敛。

该机制不是用规则控制机器人，而是为强化学习策略提供更稳定的任务语义，使模型更容易学会不同地形采用不同运动模式。

**创新点**

- 将单一速度命令扩展为地形条件化速度锚点。
- 通过高度扫描推断地形语义，避免依赖隐藏标签。
- 命令同步写入 Policy 观测、Critic 观测与环境 command manager，保证训练一致性。

## 基于评分函数对齐的多目标奖励架构

Track 任务最终评分由完成系数、时间分、姿态分和能耗分共同决定。项目围绕官方评分机制重新组织奖励函数，使训练目标与最终评测目标保持一致。

当前奖励总体比例设计为：

```text
速度/时间 : 姿态 : 电量 ≈ 2 : 2 : 1
```

核心奖励包括：

- `track_lin_vel_xy`：提升速度命令跟随能力；
- `command_speed_advantage`：鼓励实际速度达到目标，惩罚低速；
- `goal_velocity_projection`：鼓励朝终点方向有效推进；
- `navigation_time`：加大时间压力，减少绕圈和拖延；
- `pose_score_formula`：拟合官方姿态评分；
- `energy_score_formula`：拟合官方能耗评分；
- `task_complete`：强化赛道完成；
- `wall_collision`、`stuck_penalty`、`near_goal_circling_penalty`：约束迷宫无效行为。

该奖励体系避免单纯追求速度导致姿态和能耗崩塌，也避免过度追求稳定导致速度不足。模型被引导学习更快、更稳、更省电地完成任务。

**创新点**

- 奖励函数与最终评分项建立显式映射。
- 将稀疏终点任务拆解为可学习的稠密推进信号。
- 通过比例化奖励设计实现时间、姿态、能耗之间的可控平衡。

## 基于地形门控的复杂地形理解架构

系统构建了地形门控模块，基于高度扫描信息估计当前地形类型和难度，包括平缓区域、坡面、楼梯、墙体和迷宫结构。该模块输出地形类别、难度带、边缘强度、地形切换状态和速度建议，用于辅助阶段速度锚点和训练监控。

地形门控并不直接替代策略决策，而是向训练系统提供可解释的地形语义，使策略能够在同一网络中学习多种地形下的差异化运动行为。

**创新点**

- 基于公开高度扫描构建地形语义，不依赖额外隐藏信息。
- sticky gate 机制减少地形识别抖动，提高速度锚点稳定性。
- 支持训练阶段难度过滤，使样本分布更贴近目标评估任务。

## 基于 PPO 稳定性增强的训练优化架构

系统以 PPO 为核心优化算法，并针对复杂地形、多目标奖励和预训练模型微调场景进行了稳定性增强。Track 阶段采用较小学习率、较低熵系数和严格 KL 约束，避免策略更新过大导致原有稳定步态被破坏。

关键训练参数包括：

| 参数 | 当前值 | 作用 |
| --- | ---: | --- |
| 并行环境数 | 4096 | 提升采样吞吐和梯度稳定性 |
| 学习率 | 3.0e-5 | 加快新奖励适应，同时避免大幅破坏预训练模型 |
| PPO epochs | 3 | 控制每轮样本重复更新强度 |
| mini-batches | 4 | 保持稳定更新粒度 |
| desired KL | 0.004 | 限制策略更新幅度 |
| entropy coef | 0.0015 | 保留探索但抑制步态扰动 |

训练过程加入 NaN/Inf loss 跳过、异常梯度检测、梯度裁剪、value loss 归一化和 action std 限幅，提高长时间训练稳定性。

**创新点**

- 面向复杂奖励分布强化 PPO 数值稳定性。
- 通过 KL 与 std 双重约束保护预训练步态能力。
- 适配大规模并行环境训练，提高策略更新可靠性。

## 基于监控诊断闭环的系统验证架构

系统构建了多维监控面板，对训练过程进行可解释诊断。监控内容覆盖完成率、总分、时间分、姿态分、能耗分、速度误差、地形门控、迷宫碰撞、异常终止和 PPO 损失等指标。

该机制使模型调优不再依赖单一 reward 曲线，而是能够定位具体瓶颈：

- 总分低是完成率不足，还是姿态/能耗不足；
- 时间分低是速度不足，还是迷宫绕圈；
- 能耗低是速度过高，还是动作抖动过强；
- 某一难度失败率高是否来自地形采样或速度锚点不合理。

**创新点**

- 建立训练-监控-诊断-调参的闭环系统。
- 将强化学习黑箱训练转化为可观察、可解释、可迭代的工程流程。
- 支持按地形、难度和奖励项进行问题拆解。

## 系统工程核心创新总结

本项目的核心创新不在于单一算法替换，而在于围绕四足机器人复杂赛道任务构建了一套完整的系统工程方案。

**第一，目标感知导航增强。**  
通过 3 维目标特征扩展，使策略具备终点方向和距离感知能力，实现从基础运控到目标导航的能力升级。

**第二，非对称 Actor-Critic 训练结构。**  
Actor 保持轻量可执行，Critic 使用增强观测提高训练稳定性，兼顾部署可行性与学习效率。

**第三，地形自适应速度锚点。**  
针对坡面、楼梯和迷宫采用不同速度参考，使策略能够学习地形条件化行为，而不是在单一速度目标下被迫折中。

**第四，评分函数对齐奖励体系。**  
奖励设计围绕时间、姿态、能耗和完成率展开，使训练目标直接服务于最终得分。

**第五，PPO 稳定性与监控闭环。**  
通过数值稳定防护、探索噪声约束和多维监控指标，提高复杂任务训练的可靠性和可解释性。

综上，该系统形成了目标感知、地形理解、评分对齐、稳定训练、监控反馈五位一体的四足机器人强化学习工程架构，为复杂地形自主导航任务提供了高可复现性、高可解释性和高扩展性的解决方案。
